修复 React 崩溃：TaskMemoryBar 收到来自持久化/旧版本数据的部分 MemoryLine 时，goal/current/nextStep 可能为 undefined，当前直接调用 trim() 导致 TypeError。

范围严格限制：
- desktop/frontend/src/components/desktop-ui/TaskMemoryBar.tsx
- desktop/frontend/src/__tests__/desktop-ui-components.test.tsx

要求：
1. 在组件入口把三个文本字段收敛成安全字符串，后续渲染、空值判断、aria-label、Segment 均使用规范化值，不能再对未知值直接调用 trim。
2. 保持现有合法 MemoryLine 行为和类型接口；兼容运行时缺字段/undefined 的旧数据。
3. 增加回归测试，至少覆盖部分字段缺失时不抛异常并正确渲染已有字段，以及三个字段都缺失时渲染为空。
4. 运行最小相关前端测试或项目已有对应测试命令，并报告结果。
5. 不修改其他文件，不覆盖工作区已有改动，不提交、不暂存、不推送、不发布，不接触敏感信息。

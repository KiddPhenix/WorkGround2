# Feature Map

> 2026-07-27 Work 结构规划可靠性调整：分支 `developping/definition-planner-json-repair+2026-07-27`；首次提示在 system 与最终 user 尾部双重约束单一 DefinitionPlan JSON；结构解析失败后基于最近草稿有界自动修复，强调业务内容不变、仅纠正 JSON/schema，并保留显式失败与安全重试语义。状态：`done`。

> 2026-07-23 Stage 6 Desktop back repair：正常 Session 与 Work 背面互斥复用同一 SessionSurface；attempt SessionRef 按已有 tab、topic metadata、blank+resume 三层导航，失败保留 WorkCard 并可重试；WorkCapable/List/Create/Get 的迟到 ACK 均按 tab/owner/disposed 隔离。

| 功能名 | 状态 | 分支 | 负责人 | 主要文件 | 备注 |
|---|---|---|---|---|---|
| 主人决策 Skill 导出 | `in_progress` | `developping/decision-skill-export+2026-08-17` | `Codex + WorkGround2` | `desktop/decision_skill_app.go`、`desktop/frontend/src/components/DecisionCenter.tsx`、桥接与相关测试 | 在“安装 / 更新 Codex Skill”下增加“导出 Skill”，将主人提问与完成通知 Skill 导出为便于非 Codex 用户分发和安装的压缩包；用户选择保存位置，取消不报错，失败显式可重试。 |
| 可复用 Work Session 失败补偿与孤儿清理 | `done` | `developping/reusable-flow-orphan-cleanup+2026-08-17` | `Codex + WorkGround2` | `internal/work/reusable.go`、`desktop/works.go`、`desktop/tabs.go`、相关测试 | 可复用流程在 Work 尚未提交的确定性失败下回滚本次新建空 Session；已提交、状态探测失败或既有同 requestId Session 保留恢复入口。历史无 Topic 的半完成 Work Session 会归入对应 Project/Global，保留标题与 SessionPath，可安全移入回收站。新增核心用例、Reusable/Control Work 定向回归及受影响包 vet 通过；按用户要求未跑全量测试。 |
| `notify-me` 主人完成通知 | `done` | `developping/notify-me+2026-08-17` | `Codex` | `internal/skill/builtins.go`、`internal/agent/notify_me.go`、`desktop/owner_notify.go`、`desktop/decision_skill/notify-me/` | 内置 Skill 承接显式“任务结束后通知我”；Desktop 在任务结束时读取 Windows 系统最后输入时间，AFK 满 5 分钟默认创建无需回复的主人通知。同一轮显式通知会抑制自动重复，创建与重试使用稳定幂等键；成功、失败、取消均有人类可读终态。Skill 校验、相关 Go 测试、Desktop 全包、全仓 Go 测试、两套 vet、前端生产构建与 Wails 正式构建通过。 |
| 常用流程跨 Session workspace 路由修复 | `done` | `developping/reusable-flow-workspace-route+2026-08-17` | `Codex + WorkGround2` | `desktop/works.go`、`desktop/works_test.go` | `CreateReusableWorkSession` 以保存流程的源 Controller 实际 workspace 为路由依据，避免 Tab 元数据短暂漂移时新 Session 到错误 Work store 查询 flow；目标 Session 仍由自身 Controller 执行 DAG，路由冲突显式返回可恢复错误。同 requestId 幂等重放、正常 project/global 回归、Desktop 全量测试（236.884s）与 Desktop vet 均通过。 |
| 全量测试稳定性修复 | `done` | `developping/full-test-stability+2026-08-16` | `Codex + WorkGround2` | 根模块、`desktop/` 及失败测试涉及的实现与测试 | 修复 Windows receipt 并发/文件名、V2 重试投影、时间类型契约等真实缺陷；更新过时测试与 golden；前端 `test:all` 覆盖全部 127 个测试文件并以 4 个隔离进程并行。同步最新 `main` 后，根模块与 Desktop 全量、前端全量和生产构建、两套 vet 均通过。 |
| 浏览器原生弹窗适配 | `done` | `developping/browser-native-dialogs+2026-08-16` | `Codex + WorkGround2` | `internal/browser`、`internal/browser/cdp`、`internal/tool/browser`、相关测试和文档 | CDP 按 target 处理 `alert`、`confirm`、`prompt`、`beforeunload`；导航、点击、关闭 Tab 默认保护现场，`allow_leave=true` 可明确离开。关闭 Tab 先触发可观察的离开检查，再执行关闭，适配 Chrome 直接关闭绕过卸载钩子的行为。处理失败返回 outcome-unknown 并失效旧 revision。浏览器专项、Tool/Boot、build、全仓 vet 与 Chrome 151 真实集成通过；全仓测试保留 CLI、installsource、serve 的 4 项既有基线失败，race 因本机无 GCC 未运行。 |
| DSH Bundle 兼容运行时 | `done` | `developping/dsh-bundle-compat+2026-08-16` | `Codex` | `internal/pluginpkg`、`internal/installsource`、`internal/dshcompat`、`internal/boot`、`desktop/dsh_workbench_app.go`、`desktop/frontend/src/components/CapabilitiesPanel.tsx`、`docs/DSH_BUNDLE_COMPATIBILITY*.zh-CN.md` | 支持 DSH Bundle 静态安全预检、目录/zip/GitHub 安装、源目录依赖回退与 Doctor；每 WG2 Controller 启动会话所有的 Node/Cordis Agent sidecar，以 `dsh__<bundle>__<tool>` 桥接 Tool、取消和关闭。Client UI 通过 loopback-only 原生 DSH Web/React 18 Host 在 sandbox iframe 中完整映像，与 WG2 会话状态隔离。真实验证 base/headless/web-app，以及 todo、fs、pwsh cancel、skill filesystem、client-ui-goal、client-ui-skill；专项 Go/vet、前端行为/类型/CSS/生产构建和 Wails 正式构建通过。宽回归保留已有 CLI/Control/Serve、Darwin 路径、Topic/Work 存储与 Windows 临时目录基线失败。按用户要求未使用 WorkGround2 Worker。 |
| Work 重运行恢复与 URL 成果 | `done` | `developping/work-rerun-url-artifacts+2026-08-15` | `Codex + WorkGround2` | `internal/work/reusable.go`、`definition_service.go`、`internal/control/taskexec.go`、`desktop/work_artifact_file.go`、`ProjectTree.tsx`、成果架与规划器 | 可复用 Work 先提交并返回稳定 WorkID，再异步幂等唤醒 DAG；重复请求复用同一 Work，崩溃窗口由启动恢复接管，已完成 Run 不再误唤醒。历史无 Topic 的 New Work 可按 Session 路径回收。成果支持 `url/link`：规划、编辑、执行、计数、持久化、安全 HTTP(S) 校验及浏览器打开全链闭环，不生成伪文件。Work/Control/Boot/Desktop 专项、前端成果架 291 项、ProjectTree 97 项、两套 vet、类型检查及生产构建通过；全 DTO 契约仍有 4 项可在原始分支复现的既有漂移。独立 worktree 完成，未影响等待输入的发版 Work。 |
| 项目文件夹图标选择 | `done` | `developping/project-folder-icon+2026-08-15` | `Codex` | `desktop/tabs.go`、`desktop/frontend/src/components/ProjectTree.tsx`、`projectIcons.ts`、相关测试 | 项目颜色圆点保持独立；用户选择的文件夹、星标、书签、代码、终端或闪电直接占用原文件夹图标槽位，默认文件夹仍随展开状态变化。TypeScript、CSS/z-index 门禁及 Vite 生产构建通过；WorkBench 布局本功能断言通过，全套保留 12 条既有基线失败。截图驱动 UI 改动，按规则不使用 WorkGround2 Worker。 |
| Session 最近列表重命名与颜色横排 | `done` | `developping/session-list-rename-color-row+2026-08-15` | `Codex` | `desktop/frontend/src/components/ProjectTree.tsx`、`ContextMenu.tsx`、`App.tsx`、`styles.css`、相关测试 | 最近区 Topic/Session 重命名使用具体行身份隔离，普通运行时 Session 按路径改名；项目视觉标签改为单行可访问颜色选择。ProjectTree 95/95、TypeScript、CSS/z-index 门禁和 Vite 生产构建通过；WorkBench 布局测试本功能新增断言通过，全套仍有 12 条既有基线失败。截图驱动 UI 改动，按规则不使用 WorkGround2 Worker。 |
| Work Session List 重命名 | `done` | `developping/work-session-list-rename+2026-08-14` | `Codex` | `desktop/frontend/src/components/ProjectTree.tsx`、`project-tree-runtime.test.ts` | 修复单 Work Session 被建模为运行时节点后错误继承“仅回收”能力的问题；列表中的 Work 行现可通过菜单或双击改名，普通多运行时 Session 仍保持独立回收语义。ProjectTree 93 项、TypeScript、CSS/z-index 门禁和 Vite 生产构建通过。小范围前端修复，不使用 WorkGround2 Worker。 |
| 运行过程浮层浅色适配 | `done` | `developping/run-process-light-theme+2026-08-14` | `Codex` | `desktop/frontend/src/styles.css`、主题契约测试 | 修正 Workbench 运行过程窗口残留的深色硬编码，覆盖面板、活动区、步骤轨道、状态色、滚动条与阴影。Iris 89 项主题测试、CSS 门禁、生产构建及真实组件浅色截图检查通过。小范围 CSS 修复，不使用 WorkGround2 Worker。 |
| 动态壁纸场景与主题重整 | `done` | `developping/dynamic-wallpaper-refresh+2026-08-14` | `Codex` | `desktop/frontend/src/components/DynamicWallpaper.tsx`、`SettingsPanel.tsx`、`styles.css`、本地化与相关测试 | 海浪改为全屏连续水面；删除余烬、流光绸缎、雨夜霓虹并将旧配置安全回退为纹理；其余 8 个场景使用真实 Shader 静态预览并补齐独立浅色配色。配置专项、前端 45 项背景契约、TypeScript、CSS 门禁与生产构建通过；深浅主题真实 WebGL 截图检查通过。Desktop 全包测试 184 秒外层超时且未返回失败详情。用户明确要求不使用 WorkGround2 Worker。 |
| Browser State 结构化截断 | `done` | `developping/browser-state-structured-truncation+2026-08-14` | `Codex + WorkGround2` | `internal/agent`、`internal/browser`、`internal/tool`、`internal/tool/browser`、相关测试 | `browser_state` 超过 Agent 32 KiB 单次工具输出上限时，先按 rune 收缩正文、再按完整元素裁剪，保持 JSON、revision 和原始 index 有效并显式标记截断；回包提供 `next_element_index` / `remaining_elements`，可用同 revision 续页，过期或失效快照返回 `stale_state` 且不暗中刷新。三包专项、Tool/Boot 边界测试和受影响包 vet 通过；全仓测试/vet 并行门禁在 364 秒外层超时且未返回失败详情。 |
| 动态 Skill 词表激活与重建 | `done` | `developping/dynamic-skill-vocabulary+2026-08-14` | `Codex` | `internal/vocabulary`、`internal/control`、`internal/cli`、`desktop`、`desktop/frontend/src/components/Composer.tsx`、相关测试与文档 | 完整输入或选中 Skill Slash 命令时，未回车即将该 Skill 词表幂等加入当前 Session；`/rebuild_vocabulary` 扫描 Workspace，只替换项目 `.WorkGround2/vocabulary.toml` 的自动生成区并热重载当前 Session，保留手工区且检测并发修改。Vocabulary、Control、CLI 菜单和 Desktop 专项、Composer 28 项回归、两套 vet、前端生产构建及 Wails 正式构建通过；根模块与 Desktop 全量分别在 184 秒、124 秒无输出超时，CLI 全包仅有既有 Skill Picker 路径大小写和 Statusline 用例失败。按用户要求不使用 WorkGround2 Worker。 |
| Workspace 词汇表与 Session Tab 补全 | `done` | `developping/workspace-vocabulary+2026-08-14` | `Codex` | `internal/vocabulary`、`internal/control`、`internal/skill`、`desktop`、`desktop/frontend/src/components/Composer.tsx`、`docs/WORKSPACE_VOCABULARY.zh-CN.md`、相关测试 | 已实现 Workspace、Skill、Agent 与运行时学习词汇的优先级合并；自动提取限于显式/高信号短词并过滤密钥、路径与命令。学习状态私有落盘，支持原子写、跨 Session 合并刷新、崩溃锁恢复、事件/使用回执去重及明确警告。Session 输入框支持 90ms 防抖的暗色后缀和 Tab 接受，兼容 IME、`/`、`@`、`!`、Esc 与 Shift+Tab，迟到跨 Workspace 响应会被丢弃。临时定义不改动稳定 system prompt，且不污染历史预览。本功能专项、受影响 Go 包、全仓 vet、Composer 回归、CSS/TypeScript/前端生产构建及 Wails 正式构建通过；全仓与 Desktop 全包仅存既有 CLI、installsource、serve 和 Work V2 用例失败。用户明确要求不使用 WorkGround2 Worker。 |
| Browser 持久复用与 Playwright 协作 | `done` | `developping/browser-playwright-reuse+2026-08-13` | `Codex + WorkGround2` | `internal/browser`、`internal/browser/cdp`、`internal/tool/browser`、`internal/boot`、配置、测试和文档 | 每用户回环 CDP endpoint 以原子 0600 文件记录，跨进程锁、心跳和失效校验保证优先复用、失败重建；新浏览器以 OS 脱离进程启动，Driver 统一经 RemoteAllocator 控制并安全 detach，`browser_close`、Controller/Task、设置重建和应用退出均不关闭底层浏览器；`browser_attach` 供 Playwright `connectOverCDP` 复用同一实例，外部写后由 `browser_state(refresh=true)` 重建 DOM/AX 摘要与 revision。真实 Chrome 已验证页面和 endpoint 跨两次 detach/reattach 存活。 |
| 最近会话 Pin 视觉修正 | `done` | `developping/session-pin-right-edge+2026-08-13` | `Codex` | `desktop/frontend/src/components/ProjectTree.tsx`、`desktop/frontend/src/styles.css`、`desktop/frontend/src/__tests__/workbench-layout.test.ts`、`design-qa.md` | 静态 Pin 状态已移到 Recent 行最右侧 `right: 10px`，置顶行 Workspace 左移至 `right: 34px`；悬停、键盘聚焦或菜单打开时状态 Pin 给 Unpin / `…` 让位。实机两条置顶行均显示，Workspace 与 Pin 间距 10px；相关布局契约、CSS、z-index、TypeScript、Vite production 和浏览器 QA 通过，历史布局脚本保持既有 12 条漂移失败（113/125）。用户明确要求 Codex 直接修改，不使用 WorkGround2 Worker。 |
| 原生浏览器操作能力 | `done` | `developping/browser-control+2026-08-12` | `Codex + WorkGround2` | `internal/browser`、`internal/browser/cdp`、`internal/tool/browser`、`internal/config`、`internal/boot`、`desktop/settings_app.go`、`desktop/frontend/src/components/SettingsPanel.tsx`、相关测试和文档 | 已增加独立浏览器启动配置 `tools.browser.incognito` 和权限页“用隐身模式打开”开关，默认关闭；配置经 Boot/Manager/Driver 传到 CDP，仅开启时追加 `--incognito`。启动偏好与密码/上传权限接口分离；Desktop 保存会重建浏览器运行时，下次 `browser_open` 按新模式启动。既有随机非零调试端口、原生工具优先策略、Profile、权限和生命周期接口保持不变。 |
| Room Relay-only 文件接收崩溃修复 | `done` | `developping/room-relay-file-recovery+2026-08-11` | `Codex` | `desktop/collab_transport.go`、`desktop/collab_relay_host.go`、`desktop/collab_relay_file.go`、`desktop/collab_file_transfer.go`、对应测试 | LAN 关闭时不再把 typed-nil HTTP 文件通道装入 Relay fallback；缺失 peer 显式降级，文件下载 Panic 被隔离为可手动重试的失败状态，并在异常路径释放文件锁和自动接收并发槽。50 项文件/Relay、153 项 Collaboration、关键路径 race、Desktop vet、Wails clean build 通过；原“欢迎船总指导”缓存启动 20 秒无 Panic，传输恢复为 `waiting_sender`。用户明确要求 Codex 直接处理，不使用 WorkGround2 Worker。 |
| Room 导出连接路由选择 | `done` | `developping/room-invite-route-selection+2026-08-10` | `Codex` | `desktop/collab_app.go`、`desktop/frontend/src/collab/CollaborationWorkspace.tsx`、`desktop/frontend/src/collab/invite.ts`、对应测试 | 导出下拉已按 Relay、普通 LAN IP、`127.0.0.1`/`::1` 排序，Relay 房间默认选中首个 Relay；切换选项会重新编码只含所选路由的邀请，不再固定复用后端聚合字符串，同时保留 LAN V2 协议版本。Desktop Collaboration、Go 定向测试、Desktop vet、前端协作 334/334、TypeScript 与 production build 通过。用户明确要求 Codex 直接处理，未使用 WorkGround2 Worker。 |
| Room Relay 文件票据身份修复 | `done` | `developping/room-relay-file-ticket+2026-08-10` | `Codex` | `desktop/collab_relay_file.go`、`desktop/collab_relay_test.go` | Relay manifest 响应已保留原始 Offer revision，接收方可对票据和分享声明执行完整身份校验。Relay 全套、关键路径 race、Desktop vet 与 diff check 通过；Desktop 全量仍失败于未改动且可独立复现的两个 Work V2 FileWorkStore 用例。小范围后端修复，直接本地处理，不使用 WorkGround2 Worker。 |
| Room Relay 文件源注册修复 | `done` | `developping/room-relay-file-origin+2026-08-10` | `Codex` | `desktop/collab_file_transfer.go`、`desktop/collab_file_transfer_test.go` | 纯 Relay 分享现在直接由 Relay peer 校验并注册，不再错误依赖本地 HTTP file origin；HTTP/LAN 路径仍严格要求本地监听源。文件分享相关回归、关键路径 race、Desktop vet 与 diff check 通过。Desktop 全量测试失败于未改动且可独立复现的 Work V2 Session binding 用例。小范围后端修复，直接本地处理，不使用 WorkGround2 Worker。 |
| Host Room 恢复测试重构 | `done` | `developping/host-room-recovery-test-redesign+2026-08-10` | `Codex` | `desktop/collab_app.go`、`desktop/collab_app_test.go` | 已删除仅验证 `sync.Once` 的实现细节测试，改为隔离真实状态目录、写入原始 Host 缓存与 Recovery 遗留缓存、调用完整启动扫描的行为测试；断言同一逻辑 Room 仅调度一次恢复、runtime 使用原始 owner path 且保留完整 Host authority。Collaboration 宽回归、定向 race、vet 与 diff check 通过。用户明确要求由 Codex 直接处理，不使用 Worker。 |
| Host Room 恢复稳定性 | `done` | `developping/host-room-recovery-stability+2026-08-10` | `Codex` | `desktop/collab_app.go`、`desktop/collab_persist.go`、`desktop/collab_app_test.go` | recovery Session、缓存加载与启动扫描统一使用原始逻辑 owner path；仅存旧 recovery-path Room 缓存时会迁移到稳定位置，遗留重复缓存由 runtime 一次性闸门保证只启动一次 Host 重连。Desktop Collaboration 宽回归、定向 race、vet 与 diff check 通过。按用户偏好由 Codex 直接处理，不使用 WorkGround2 Worker。 |
| 最近列表已读回执修复 | `done` | `developping/recent-unread-receipt-fix+2026-08-10` | `Codex` | `desktop/frontend/src/components/ProjectTree.tsx`、`project-tree-runtime.test.ts` | 最近行点击现在通过单一同步入口幂等持久化可见活动水位，并同时推进统一未读会话游标，避免导航卸载丢失本地回执或依赖迟到 active effect；覆盖普通 Session、Work 与 Crew/Room 路径。ProjectTree 91/91、TypeScript、CSS/z-index 与 Vite production build 通过。Codex 直接处理，未使用 WorkGround2 Worker。 |
| Work / Room 侧栏恢复入口 | `done` | `developping/work-room-sidebar-restore+2026-08-10` | `Codex` | `desktop/frontend/src/App.tsx`、`styles.css`、`workbench-layout.test.ts` | App 在侧栏收起且处于 Work/Room 时统一渲染左上恢复按钮，覆盖正常、初始化、不可用和未连接态；侧栏内收起按钮向右移动 8px。新增契约、CSS、Room 286 项、TypeScript 与 Wails clean build 通过，`debug-restart.bat` 已成功重启新 EXE；布局脚本其余 12 项及 Work 宽套件失败为既有漂移。按用户要求直接实现，不使用 WorkGround2 Worker。 |
| 最近列表 Session 回收站修复 | `done` | `developping/recent-session-trash+2026-08-09` | `Codex` | `desktop/frontend/src/components/ProjectTree.tsx`、`desktop/frontend/src/__tests__/project-tree-runtime.test.ts` | “最近”和“项目”重复节点的菜单实例改为按 section + node key 隔离，避免两个 ContextMenu 互相关闭；最近行新增显式操作入口，Topic/Work Session 继续整 Topic 回收，普通 Runtime/Crew Session 按路径回收单个 Session。前端定向测试 53 项、TypeScript 类型检查及 production build 通过。 |
| Session List 上下边缘渐隐 | `done` | `developping/session-list-edge-fade+2026-08-10` | `Codex` | `desktop/frontend/src/styles.css`、`desktop/frontend/src/__tests__/workbench-layout.test.ts` | 工作台 Session List 滚动区已增加上下 18px 透明渐隐，透出当前侧栏背景且不增加拦截交互的覆盖层。新增契约通过，CSS、TypeScript 与 Vite 生产构建通过；布局脚本其余 12 项既有断言失败与本次无关。按用户要求直接实现，不使用 WorkGround2 Worker。 |
| Session 已读身份稳定化 | `done` | `developping/unread-attention-sources+2026-08-08`（复用当前分支） | `Codex` | `desktop/frontend/src/components/ProjectTree.tsx`、`project-tree-runtime.test.ts` | Topic 已读身份已收敛到 scope/workspace/topic，不再跟随恢复 transcript 的 `sessionPath` 漂移；具体 Runtime Session 仍按规范化路径独立记录。ProjectTree 84/84、TypeScript、CSS/z-index 与 Vite 生产构建通过。用户明确要求直接处理，不使用 Worker。 |
| Windows 无边框窗口点击命中修复 | `in_progress` | `developping/unread-attention-sources+2026-08-08`（复用当前脏分支） | `Codex` | `desktop/frontend/src/App.tsx`、`styles.css`、`app-chrome-tabs.test.ts` | 移除全视口 resize 包装层；窗口按钮与设置入口隔离 `pointerdown/mousedown`、在 `pointerup` 执行鼠标动作并保留键盘 click；桌面主界面把 Wails v2 基于 `outerWidth/clientX` 的全局边缘推断阈值设为负无穷，只保留 8px 边缘和 18px 角落显式缩放命中区，避免高 DPI 下正文右侧大面积误判为缩放，同时保留 Wails 状态机。隔离 GUI 已验证设置点击、普通窗口→最大化→恢复，以及仅最后 8px 显示横向缩放并可拖动；正式版仍按用户要求未重建。当前工作树已有协作模块未提交改动，未切分支或提交。 |
| 总体未读事件扩展 | `done` | `developping/unread-attention-sources+2026-08-08` | `Codex` | `internal/unread`、`desktop/unread_app.go`、Session/Work 事件接入、相关测试 | 非 CLI 普通 Session 在 LLM 完成、失败、提问或等待审批时写统一未读；Work 在聚合完成、输入/审批等待或规划澄清时写一个可重放去重的 Work 未读，避免与 Work Session TurnDone 重复。当前活动 Session/Work/Room/IM 只写已读回执，不产生数字，重连重放也不会复活。定向 Go、Race、Vet、前端类型检查及最近列表 48 项契约通过；根模块和 Desktop 全量测试分别运行 6/4 分钟无失败输出后超时。按用户要求由 Codex 直接实现，未使用 WorkGround2 Worker。 |
| 总体未读表现层 | `done` | `developping/unread-attention-sources+2026-08-08`（复用当前分支） | `Codex + WorkGround2（本轮 Codex）` | `desktop/frontend/src/components/ProjectTree.tsx`、`desktop/frontend/src/styles.css`、`desktop/unread_app.go`、`desktop/tabs.go`、相关测试 | Windows 任务栏角标与 Recent 共用 Summary；所有未读强制进入“最近”，上限只限制已读补位项。旧 UUID Session 未读按 path、当前 runtime UUID、文件 stem、唯一标题与完成时间证据恢复，成功后幂等自愈为 `path:`，并严格执行“解析→导航成功→清未读”。本轮进一步拆分状态点语义：绿色实心点只表示已完成但未查看，运行中使用独立动态环，低动态偏好下保持静态环，避免运行开始/结束造成假未读观感。新增 3 项状态样式契约、ProjectTree 81 项测试、CSS/z-index、typecheck 与生产构建通过；`workbench-layout` 保持既有 12 项无关失败且本轮契约全部通过。用户明确要求本轮不使用 Worker。 |
| 总体未读数据层（Room / IM） | `done` | `developping/overall-unread-data+2026-08-08` | `Codex` | `internal/unread`、`internal/bot`、`desktop/unread_app.go`、`desktop/collab_*`、`desktop/bot_runtime_app.go` | 已新增原子持久化统一未读仓库：Room 以 Timeline Sequence 首次建基线、重连补读并过滤本人成果/系统噪声；IM 在 allowlist/self 过滤后、命令/排队/Agent 处理前按 MessageID 幂等记账并分配本地序号，失败显式阻断且可重试。提供 Summary、单调已读游标、Session 绑定、revision 与 `unread:state` 后端事件，未接 UI。相关包及 Desktop 定向单测/race/vet 通过；根模块与 Desktop 全量回归仍失败于未改动的 Work 投影、Darwin 路径和 Work V2 用例。按用户要求由 Codex 直接实现，不使用 WorkGround2 Worker。 |
| 动态壁纸主题扩展 | `done` | `developping/dynamic-wallpaper-polish+2026-08-08` | `Codex` | `desktop/frontend/src/components/DynamicWallpaper.tsx`、视觉审计截图与相关测试 | 已将余烬、星空、黑洞、月夜云海、深海荧光、流光绸缎、沙海暮光、雨夜霓虹 8 个主题按主体构图、材质、景深、光照和运动层次重塑，并完成逐主题真实 WebGL 截图复查；前端 46 项契约、CSS、类型检查及 Go 背景相关测试全部通过。视觉调整由 Codex 直接完成，跳过无法观察 GPU 画面的 Worker。 |
| 黑洞动态壁纸参考图匹配 | `done` | `developping/blackhole-reference-match+2026-08-08` | `Codex` | `desktop/frontend/src/components/DynamicWallpaper.tsx`、`design-qa.md`、参考图与实机截图 | 已按用户图二重做：缩小事件视界，移除橙色厚盘与重复径向条纹，改为白金色上下透镜弧、窄暗隙、极薄水平吸积线和蓝黑空间柔光。使用独立 worktree 隔离另一未完成分支及用户改动；完成原始 WebGL、桌面遮罩态、等尺寸并排对照与浏览器日志检查，Design QA 通过。 |
| 黑洞前景透镜光弧修正 | `done` | `developping/blackhole-lensed-front-arcs+2026-08-08` | `Codex` | `desktop/frontend/src/components/DynamicWallpaper.tsx`、`design-qa.md`、参考图与实机截图 | 已恢复用户认可的上一版橙金吸积盘、事件视界比例、星场和整体构图，仅在黑洞下半部补充三道同色系前景透镜光弧；未调整其他主题。完成标注图/实机图全景及局部并排复查，浏览器无告警错误，背景契约 46/46、CSS、TypeScript、Vite 生产打包及标准 `debug-restart.bat` 均通过，新桌面进程已启动。 |
| Room Snapshot 分块同步与离线合流 | `done` | `developping/session-list-redesign+2026-08-07`（复用当前脏分支） | `Codex` | `internal/collab`、`desktop/collab_snapshot*`、`desktop/collab_projection.go`、`desktop/collab_transport.go`、`desktop/collab_relay_*`、协议文档与对应测试 | 已新增固定 Sequence 水位的 Manifest/Chunk Snapshot：512 KiB 默认块、逐块/整体 SHA-256、5 分钟可续期有界缓存、四路下载、逐块重试、单 staging 缓冲完整校验后原子切换，旧 Host 自动回退 `/snapshot`；LAN V1/V2 与 Relay RPC 均接通。普通事件改为本地投影，缺口或旧成员事件信息不足时补读/回退 Snapshot。两个非 Host Client 的持久 Outbox 在 Host 恢复后按单端 FIFO、跨端到达顺序合流，原 RequestID 重放幂等。`internal/collab` 全量、Desktop Collaboration/Relay 定向、双方定向 race/vet、前端协作回归 286/286、TypeScript 类型检查、格式与 diff check 通过；根模块和 Desktop 宽套件仍在未改动的 Serve/Work/Darwin 路径与 Session/Work 测试失败或长时超时。按用户要求由 Codex 直接实现，不使用 WorkGround2 Worker。 |
| 小组件多皮肤操作区精修 | `done` | `developping/session-list-redesign+2026-08-07`（复用当前脏分支） | `Codex` | `desktop/frontend/src/components/widget/WidgetMode.tsx`、`widget-mode.css`、`widget-skins.css`、`assets/widget-mode/skins/pet/control-*-v2.png`、布局契约与 `design-qa.md` | 根据实机反馈重做按钮边缘系统，并为电子宠物皮肤重新生成暗绿操作槽、薄荷副键、杏橙主键三张无字 PNG 图素；淘汰首轮厚框/双沟槽结果，第二轮收为单一薄压边，透明化后以 filled nine-slice 接入，DOM 继续承载文字与图标。590×176 对照确认透明角、切片接缝、右缘、文字和按钮位置正常；布局/切片专项、TypeScript、CSS、Vite production、scoped diff check 与独立 Wails production 构建通过，验收版 `desktop/build/bin/WorkGround2-widget-raster.exe`。按用户要求直接实现，不使用 WorkGround2 Worker。 |
| Room 小文件自动接收与图片直显 | `done` | `developping/session-list-redesign+2026-08-07`（复用当前脏分支） | `Codex + WorkGround2` | `desktop/collab_file_transfer.go`、`desktop/collab_agent.go`、`desktop/collab_relay_file.go`、`desktop/frontend/src/collab`、对应测试 | 他人分享且严格小于 1 MiB 的文件会按 Room 实例幂等接收到所属 workspace 的 `.workground2/attachments/room/`，完成并复核 SHA 后以安全相对 `@` 路径提供给被引用的 Agent；1 MiB 及以上维持手动接收，无 workspace 时明确提示。PNG/JPEG/GIF/WebP 经普通文件、大小、SHA、真实 MIME、静态帧与 12MP 上限校验后在消息内懒加载直显；预览并发、队列、DOM 驻留和 LRU 均有界。自动传输具备 Snapshot 补读、断线恢复、公平退避、持久配额、无覆盖暂存、代理限定和 Room authority 隔离。Desktop Room 全量相关测试、race、vet、`internal/collab`、前端 286 项、TypeScript 与生产构建通过。当前分支已有 Session List 未提交改动，为避免搬移用户工作树未另切分支。 |
| Session List 层级与选中态重做 | `in_progress` | `developping/session-list-redesign+2026-08-07` | `Codex` | `desktop/frontend/src/components/ProjectTree.tsx`、`desktop/frontend/src/styles.css`、本地化、前端测试、`design-qa.md` | 已按参考图重做“最近 / 项目 / 会话”层级，项目卡与唯一选中行恢复透明底色；移除工作台会话行 `…` 和“项目”标题栏 `⋮`，保留新增项目、项目行菜单、会话右键菜单及 Recent Pin。Recent 现在稳定可见，可配置是否显示外部调用及 `1/3/5/10` 条；CLI、IM/Bot、外部导入按来源字段判定，Work/多人协作不误判，设置本地持久化并经完整进程重启验证。修复 `global_work_session` 作用域错误及旧 Workspace `display:none` 覆盖；正在修正 Windows 无边框窗口底部 resize 命中层覆盖 Session List 最后一个操作入口的问题。按用户要求由 Codex 直接实现，未使用 WorkGround2 Worker。 |
| Room 指令与 Skill 弹层可见性 | `done` | `developping/room-agent-context-modal+2026-08-07` | `Codex` | `desktop/frontend/src/collab/collab.css`、协作测试 | 修复工作台直接子元素规则覆盖配置弹层 fixed 定位，导致点击后弹层被 Room 裁切的问题；配置弹层以更具体规则恢复窗口级 fixed 定位，并统一使用全局 modal 层级。协作专项 253/253、CSS 检查、TypeScript 与 Vite 生产构建通过；局部点击回归，未使用 WorkGround2 Worker。 |
| Room 他人 Agent 委派菜单 | `done` | `developping/room-agent-request-menu+2026-08-07` | `Codex + WorkGround2` | `desktop/frontend/src/collab`、`desktop/frontend/src/locales/collab.ts`、协作测试 | 保留本机 Agent 响应入口，将重复的“更多/同意并执行”替换为双机器人图标委派菜单；仅列出在线的其他成员，按“主人名字 · Agent 名字 · 职责”展示并复用既有请求链路，职责缺失显式提示。弹层按滚动区空间上下翻转，支持外部点击、Escape 与焦点恢复；Agent Role 从后端 Snapshot 完整归一化。252 项协作断言、TypeScript、CSS 检查、Vite 生产构建与 diff check 通过。 |
| Room LAN V2 单端口复用 | `done` | `developping/room-lan-v2-port-multiplex+2026-08-07` | `Codex` | `docs/ROOM_LAN_V2_PROTOCOL_DESIGN.zh-CN.md`、`internal/collab`、`desktop/collab_*`、`desktop/frontend/src/collab` | 新增 Room-scoped V2 HTTP/SSE/文件传输路径，以稳定自动生成的 `room_id` 在 App 级共享 Listener、Service、Hub 与 FileStore 上路由多个 Room；关闭单 Room 只注销路由。创建页前置房间名与描述、隐藏内部 ID 并默认 V2；LAN Route 显式携带协议版本。V1 Handler、旧邀请和缺版本持久化状态继续按 V1 工作。协议包全量、Desktop Collaboration 全量、双方 vet、229 项前端协作回归、TypeScript/CSS 与生产构建通过；根模块与 Desktop 全量测试分别在 6/5 分钟外层超时且无失败输出。按用户要求未使用 WorkGround2 Worker。 |
| 内部提示词与 UI 归属优化 | `done` | `developping/internal-prompt-quality+2026-08-07` | `Codex` | `internal/agent`、`internal/control`、`internal/provider`、Desktop 历史展示 | 精简低质量只读收敛、Executor、Goal、计划批准、轮次上限与 Work 质量复核提示，对关键约束使用简短中英重复；新增 `Message.Origin` 区分用户输入和宿主合成消息，UI 优先按归属隐藏并仅对旧会话回退前缀匹配，CLI 出站前移除本地元数据。受影响包测试及根目录/Desktop `go vet ./...` 通过；两套完整 `go test ./...` 各运行约 3 分钟后超时且无失败输出。按用户要求不使用 WorkGround2 Worker。 |
| Room Relay URL 信任匹配 | `done` | `developping/relay-url-trust-match+2026-08-07` | `Codex` | `desktop/collab_relay_peer.go`、`desktop/collab_relay_transport.go`、`desktop/collab_relay_test.go` | 加入路线携带 URL 时，按规范化完整 URL 复用本机 Relay 的启用状态、Token 环境变量与 `allow_insecure`，不再要求各客户端填写相同 ID；ID 仅用于展示及无 URL 旧路线兼容。同 ID、不同 URL 不继承授权，未配置公网 `ws://` 继续拒绝。Relay 专项、回归测试、Desktop vet 与 diff check 通过；小范围后端修正，直接本地完成。 |
| 外观配色与内置背景收敛 | `done` | `codex/session-surface-redesign`（复用当前脏分支，未切分支） | `Codex` | `desktop/frontend/src/lib/theme.ts`, `src/components/SettingsPanel.tsx`, `src/components/SessionBackground.tsx`, `src/assets/session-pattern-{light,dark}.svg`, `src/styles.css`, `internal/config/{config,edit,render}.go`, `desktop/session_background_app.go` | 普通 Session 标题区由 84px 收紧到 64px，摘要区由 44px 收紧到 36px，并清除标题元素的浏览器默认外边距；实测标题与摘要文字间距 11.14px，右上窗体轨道仍保持 y=15px。纯色持久化、淡纹理及 ThemeStyle 接口保持不变；26 项 Session 外观契约、CSS、TypeScript、Vite 生产构建、真实页面视觉验收和独立 Wails 构建通过，验证版：`desktop/build/bin/WorkGround2-session-spacing.exe`。按用户要求不使用 WorkGround2 Worker。 |
| 性能压力仅写日志 | `done` | `codex/session-surface-redesign`（复用当前脏分支，未切分支） | `Codex` | `desktop/frontend/src/lib/crash.ts`, `desktop/frontend/src/__tests__/crash-reporting.test.ts` | 已移除长任务、事件循环延迟和 JS 堆压力的提示弹窗及发送/复制路径；保留监控、10 分钟节流、诊断上下文与 breadcrumbs，优先写入 Wails `LogWarning`，失败显式告警并回退 `console.warn`。专项测试 45/45、TypeScript、CSS、Vite production、diff check 与 Wails production 构建通过；验证版为 `desktop/build/bin/WorkGround2-performance-log.exe`。当前正式 EXE 正在运行，未强制关闭或覆盖；小范围调整，不使用 WorkGround2 Worker。 |
| Work 右上窗体轨道归位 | `done` | `codex/session-surface-redesign`（复用当前脏分支，未切分支） | `Codex` | `desktop/frontend/src/styles.css`, `src/__tests__/workbench-layout.test.ts` | 已将 Work 标题栏的模糊迁移到纯绘制伪元素，避免 `backdrop-filter` 把 fixed 操作区重置为标题栏坐标；“会话/基石”和原生窗体按钮恢复同一窗口基线，Session 与 Room 未改。目标 CSS 契约 3/3 通过，CSS/TypeScript/前端生产构建通过；Wails 使用已验证的 `dist` 重建成功。全量 Wails 前端复编译另被并行中的 `src/lib/crash.ts` 未完成改动阻塞，与本项无关。小范围 UI 调整，按用户要求不使用 WorkGround2 Worker。 |
| Work 成果类型化图标 | `done` | `codex/session-surface-redesign`（复用当前脏分支，未切分支） | `Codex` | `desktop/frontend/src/work/components/v2/ResultCard.tsx`, `ResultShelf.test.tsx` | 已移除待生成成果的重复占位说明；有文件时按 MIME/扩展名区分 PDF、Word、表格、代码、图片、音频、视频、压缩包等图标，待生成时按章节、初稿、修订稿、最终稿、故事等成果语义选图标。成果专项 272/272、TypeScript、CSS、Vite production 与 Wails production 构建通过；正式 EXE 已更新。小范围 UI 调整，按用户要求不使用 WorkGround2 Worker。 |
| 会话正文多媒体展示 | `done` | `codex/session-surface-redesign`（复用当前脏分支，未切分支） | `Codex` | `desktop/frontend/src/components/{MarkdownRenderer,MessageImage,Message,Transcript}.tsx`、`desktop/frontend/src/lib/messageMedia.ts`、`desktop/frontend/src/styles.css`、`desktop/frontend/src/__tests__/message-media-display.test.tsx` | Markdown 图片和回复中明确提及的已登记本地图片产物可直接进入会话正文；继续通过 tab + artifact ID 安全读取，支持加载失败重试、点击大图、重复抑制，HTTP(S) 图片可直接展示。新回归 12/12、既有图片/附件/消息/渲染回归 70/70、TypeScript、CSS、Vite production、Desktop 图片专项、vet 与 Wails production 构建通过。按用户要求不使用 WorkGround2 Worker；当前分支含既有未提交界面改动，未另建分支或提交。 |
| Session 运行过程操作行 | `done` | `developping/session-process-action+2026-08-06` | `Codex` | `desktop/frontend/src/components/{Transcript,Message,SessionSurface}.tsx`、`desktop/frontend/src/components/desktop-ui/{RunBlock,IrisInfoComponents}.tsx`、`desktop/frontend/src/styles.css`、相关测试与 `design-qa.md` | 取消终态结果/过程翻面；结论只保留普通回复。运行完成后将“运行过程”收敛为钉选记忆、分叉会话、总结、回溯同级操作，默认收起、按需原位展开；运行中继续显示实时过程窗口。按用户要求未使用 WorkGround2 Worker。已通过 316 项聚焦断言、TypeScript/CSS 检查、Vite 生产构建、Wails 构建与浏览器交互验收。 |
| Room Agent 后台常驻响应 | `done` | `developping/room-agent-handoff-references+2026-08-05`（复用当前脏分支，未切分支） | `Codex + WorkGround2` | `desktop/app.go`、`desktop/collab_scheduler*`、`desktop/collab_app.go`、`desktop/collab_transport.go`、`desktop/frontend/src/collab/useCollabController.ts`、协作测试 | 自动响应调度已收敛到每个 Room Session 的 Go runtime；切换页面后继续处理 @Agent、外部问题/请求与定向 handoff，Desktop 启动会仅为当前注册的 Room Session 恢复连接和 scheduler，关闭过的缓存不会生成幽灵 Agent。message/interval/off、发条时间边界、跨重启响应间隔、稳定请求 ID、重复 snapshot 去重、有界队列和失败可观察/重试均已覆盖。Desktop Collaboration/Scheduler 专项、scheduler race、internal collab/relayserver、双方 vet、前端协作 226/226、TypeScript 与 diff check 通过；因同分支同时存在其他未提交功能改动，未 commit/merge。 |
| Work 步骤可选 Skill | `done` | `developping/room-agent-handoff-references+2026-08-05`（复用当前脏分支，未切分支） | `Codex + WorkGround2` | `internal/work`、`internal/control`、`desktop/works*`、`desktop/frontend/src/work`、Work 前端组件与测试 | 每个 Work V2 步骤可通过弹窗附加已有 Skill、清除选择或新建项目 Skill；未指定时保持原执行逻辑，指定 Skill 只追加到原步骤上下文。绑定由 V2 事件持久化并以稳定 requestId 支持安全重试；缺失/禁用 Skill 显式进入可重试失败。Work/Control/Boot/Desktop 定向测试与 vet、TypeScript、Work 契约、CSS、ExecutionList 275 项交互回归通过。 |
| Room 语义意图分类可靠性 | `done` | `developping/room-agent-handoff-references+2026-08-05` | `Codex + WorkGround2` | `internal/agent`、`internal/boot`、`internal/config`、`desktop/collab_app.go`、`desktop/frontend/src/collab/useCollabController.ts`、对应测试 | 分类失败、超时、非法响应或分类器不可用时不创建任务卡；内部超时 3 秒放宽到 10 秒、Desktop 外层 12 秒；新增 `agent.semantic_intent_model`，显式模型必须已配置且无 reasoning capability，未配置时只自动选择已配置的轻量无推理模型，不回退主 Session 推理模型。Agent/Boot/Config/Desktop 定向测试、双方 vet、前端协作 210/210 与生产 TypeScript 通过。 |
| Room Agent 定向接力与引用收敛 | `in_progress` | `developping/room-agent-handoff-references+2026-08-05` | `Codex` | `internal/collab`、`desktop/collab_agent.go`、`desktop/collab_app.go`、`desktop/frontend/src/collab`、协作测试 | 调整现有 Agent 间自动协作：每次 Room Agent 运行隐式加载在线 Agent/角色与结构化 handoff 契约，只有被明确要求响应的目标 Agent 才自动接力；主时间线按 runId 合并 Run/Result，新增 `agentRunOutput` 字段作为权威最终输出（不受旧 progress/summary 覆盖），最终输出进入普通聊天正文流并复用相同排版，不再嵌套边框、背景或独立滚动区，完成状态与指令仅作轻量元信息；失败/取消无输出时仍显示 error/status；回复改为结构化引用，长引用默认三行并支持展开与跳转；窄窗口下右栏通过缩短滚动视口持续避让 Windows 控制区，Room 与引用/回复配色统一继承 Settings 全局主题。相关 CSS 契约、TypeScript 与 Vite production build 已通过。 |
| Room Relay JoinRef 重连凭据恢复 | `done` | `developping/room-agent-handoff-references+2026-08-05` | `Codex + WorkGround2` | `internal/relayproto`、`internal/relayserver`、`desktop/collab_relay_*`、`desktop/collab_transport.go` | 公开房间首次以单次 JoinRef Attach 成功后，由 Relay 仅向 Guest 下发可复用 Guest Capability；Desktop 通过既有 secret-ref 保存，断线后不再以空 capability 重连。既有缺失凭据的 Discovery 缓存会按公开房间 ID 领取新 JoinRef 自愈；签发失败不会提前消耗 JoinRef，JoinRef 仍严格单次使用，明文凭据不进入协作 JSON/公共状态。Relay/协议专项、Desktop Relay 集成/race 与双方 vet 通过。 |
| Room 协作整合发布 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex + WorkGround2` | `internal/collab`、`internal/relayproto`、`internal/relayserver`、`cmd/workground2-relay`、`desktop/collab_*`、`desktop/frontend/src/collab` | Relay、多 Workspace 同房、双身份头像、Agent 队列/运行可见性/停止、ask/auto/yolo、自动委托、文件分享与旧 Room 恢复已统一收口。受影响 Go 包及 Desktop race、双方 vet、前端协作 200/200、Relay 设置契约、TypeScript、Vite 与 Wails production 构建通过。Root、Desktop 和前端宽套件的既有 Work/Serve/Boot/CLI/InstallSource/Composer 失败均已在干净 `origin/main@25b27ac` 或未改动文件中复现；最终产物为 `desktop/build/bin/WorkGround2-main-collaboration.exe`。 |
| Room JSON 瞬时截断恢复 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex + WorkGround2` | `internal/fileutil/atomicwrite*`、`desktop/collab_persist.go`、对应测试 | Windows ReplaceFile 降级已改为“同目录临时文件 + 原子替换”，不会再原地截断可见目标；Room 读取对瞬时 I/O、空文件和 unexpected EOF 有界重读，稳定损坏继续显式报错且不删除缓存。原子并发压力、持久化恢复、完整 Collaboration/race、双方 vet、Linux/macOS 交叉编译和 Wails 正式构建通过；产物为 `desktop/build/bin/WorkGround2-room-json-recovery.exe`。 |
| Room 临时自动审批不覆盖用户模式 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex + WorkGround2` | `desktop/app.go`、`desktop/collab_app.go`、`desktop/tabs.go`、`desktop/collab_app_test.go` | 自动委托继承用户审批强度：`ask` 单次提升为 `auto`，`auto` 保持，`yolo` 保持且不会降级后反复要求工具确认。临时执行态通过原子标志与持久态隔离，TabMeta、Room UI、autosave、BranchMeta/恢复配置继续读取用户选择；运行中显式切换 ask/auto/yolo 会取消旧恢复权，任务结束不覆盖。Desktop Collaboration、定向 race/vet、前端 200 项协作回归、TypeScript 与 Wails production 构建通过；验证版：`desktop/build/bin/WorkGround2-room-auto-delegation-yolo.exe`。已随本次版本合入主干。 |
| Room 我的 Agent 运行可见性与停止 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex + WorkGround2` | `desktop/collab_agent.go`、`desktop/collab_app.go`、`desktop/frontend/src/collab`、协作测试 | “我的 Agent”面板增加常驻运行卡，显示当前指令、可公开进度、运行/等待确认/停止中状态、开始时间和同 Session 队列数；“停止当前工作”按 Session/Run ID 精确取消所属 Controller，重复调用幂等，取消不可用时恢复原状态，最终发布 cancelled、清理状态并继续队列。Desktop Collaboration 专项、停止 race、Desktop vet、前端协作 195/195、TypeScript/CSS 与生产构建通过；已随本次版本合入主干。 |
| Room 本地打开与多 Workspace 同房 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex + WorkGround2` | `desktop/collab_app.go`、`desktop/collab_persist.go`、`desktop/frontend/src/collab/useCollabController.ts`、协作测试 | Room 缓存改按稳定 SessionPath 隔离，重开 Session 时不再受随机运行时 SessionID 影响；旧 v2 缓存按唯一 Room/Session 标题原子迁移，重名歧义显式报错且不串房。Host/Client 缓存会在网络重试前立即渲染，失败仅保留非阻塞重试状态；不同 Workspace 可各自持有同一 Room 的独立成员身份。Desktop Collaboration、定向 race/vet、前端 200 项协作回归、TypeScript/CSS、production 与 Wails 构建通过；验证版：`desktop/build/bin/WorkGround2-room-local-open-v2.exe`。已随本次版本合入主干。 |
| Room 多路由成员冲突恢复 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/collab_transport.go`、`desktop/collab_app_test.go` | 多路由聚合保留 LAN/Relay 的类型化失败原因，使外层能识别 `resume_required`，自动切换为稳定的房间作用域成员/Agent ID 并重试；聚合错误仍维持原可重试语义，其他鉴权错误不会触发身份轮换。真实 LAN Host 冲突恢复、Desktop 协作专项、定向 race、vet、diff check 与 Wails production 构建通过；验证版：`desktop/build/bin/WorkGround2-room-resume-route-fix.exe`。按用户要求不使用 WorkGround2 Worker。 |
| Room 邀请异常防崩溃 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex + WorkGround2` | `desktop/collab_app.go`、`desktop/frontend/src/collab/{invite.ts,CollaborationWorkspace.tsx}`、协作测试 | 纯 Relay 路由尚未就绪时禁止返回无效 V1 邀请（端口越界时返回可重试错误）；前端 `tryBuildCollaborationInvite` 在 render 中安全降级，不完整邀请显示错误并禁用复制。Go 覆盖 V2 成功/无 hostKey/无 routes，前端覆盖 4 种无效输入、有效 V1 与 UI 失败态；定向 Go、184 项协作回归及 TypeScript 检查通过。 |
| Room 创建表单房间名同步 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/frontend/src/collab/components/ConnectionPanel.tsx`、协作测试 | 创建 Room 时以必填的 Room 名称为默认展示名称；可选“房间信息”未填写名称时自动同步，公开 Room 不再因折叠区空字段阻止创建；单独修改展示名称时仍保留覆盖值。TypeScript、178 项协作回归、Vite 与 Wails production 构建通过；验证版：`desktop/build/bin/WorkGround2-room-name-sync.exe`。按用户要求不使用 WorkGround2 Worker。 |
| Room Agent 频率化自动协作 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/collab_app.go`、`desktop/collab_persist.go`、`desktop/frontend/src/collab`、对应测试 | 增加可持久化的 Agent 间自动协作开关与响应频率；其他 Agent 的结果按周期最多 8 条合并为一次本机 Agent 任务，可继续讨论、调试或角色模拟。协作发条默认 12 格、可配置 1..100：无人值守接力消耗格数，人类消息/操作或“上发条”会持久化重置；可切换“无限制”，但响应频率仍生效。右栏协作区可折叠并保留开关/状态摘要。保留引用去重、禁止自响应、稳定请求 ID 与失败可重试。Go 配置专项、TypeScript、176 项协作回归、Vite 与 Wails production 构建通过；验证版：`desktop/build/bin/WorkGround2-agent-collaboration-controls.exe`。按用户要求不使用 WorkGround2 Worker，已随本次版本合入主干。 |
| Room Relay Server 页签 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/frontend/src/collab/components/ConnectionPanel.tsx`、`useCollabController.ts`、`transport.ts`、协作样式/多语言/测试 | 已在加入与创建 Room 旁增加 Relay Server 页签，可直接增删、启用并保存多个 Relay，配置优先级、Discovery、Token 环境变量和不安全 ws:// 显式许可；活跃 Room 查询集中到该页，选择结果会回填 RouteSet/Host Key/JoinRef 并切回加入页。复用 Settings 单一配置源，保存与查询均带代次隔离。新增 Relay 默认匹配无参数本机服务的 `ws://127.0.0.1:8443`；WSS/明文服务不匹配时明确提示且不自动降级，Discovery 失败不会再形成全局未处理 Promise。TypeScript、159 项协作回归、Relay Settings 合约、CSS 检查、正式前端构建、Desktop Relay/Settings/Collaboration 专项及 diff check 通过。按用户要求不使用 WorkGround2 Worker。 |
| Room 跨局域网 Relay | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex + Subagents` | `docs/ROOM_RELAY_SERVICE_DESIGN.zh-CN.md`、`internal/relayproto`、`internal/relayserver`、`cmd/workground2-relay`、`internal/config`、`desktop/collab_relay*`、Desktop Settings 与协作 UI | 已实现可选 0..N Relay、LAN/Relay 多路由、Capability/E2E、Room 广告发现、一次性 JoinRef、文件中转和 Desktop 适配；Settings 可直接管理多个 Relay 及 `allow_insecure`，非回环公网 `ws://` 默认拒绝。Relay/配置/协作专项、race、vet、构建、TypeScript、153 项前端协作测试与 diff check 通过；全量 Go 套件因仓库既有长时间无输出而终止，受影响包均已通过。按用户要求不使用 WorkGround2 Worker，已随本次版本合入主干。 |
| Room Agent 配置面板视觉规范 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/frontend/src/collab/CollaborationWorkspace.tsx`、`desktop/frontend/src/collab/collab.css`、`desktop/frontend/src/locales/collab.ts`、`desktop/frontend/src/__tests__/collaboration.test.tsx` | 将右栏固定为 328px，表单统一 38px，审批条统一 36px/28px，自动响应改为 32×18 开关，并重做字号、间距、折叠区和队列层级；真实 Wails 窗口视觉验收通过，前端类型检查及协作回归 146/146 通过，验证版：`desktop/build/bin/WorkGround2-room-agent-panel-ui.exe`。按用户要求直接处理，未使用 WorkGround2 Worker。 |
| Room Agent 统一运行配置面板 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/collab_app.go`、`desktop/collab_agent.go`、`desktop/collab_persist.go`、`desktop/frontend/src/collab`、协作测试 | “我的 Agent”统一展示并控制当前 Session 模型、`ask/auto/yolo` 工具审批、自动响应、当前已加载的 `AGENTS.md` 与显式启用的 `SKILL.md`。模型/审批复用普通 Session 单一可信源；选中的 AGENTS 作为明确路径引用，选中的 Skill 由 Controller `RunSkill` 真正展开到每次 Room Agent 输入；配置和排队任务快照本机持久化、不进入 Room 协议。被删除的旧来源仍显示为“已失效”并可取消，运行时缺失显式失败且队列继续恢复。Desktop 专项与 race/vet、TypeScript、144 项协作回归、diff check、Wails production 构建通过；验证版为 `desktop/build/bin/WorkGround2-room-agent-config.exe`。直接处理，不使用 WorkGround2 Worker，已随本次版本合入主干。 |
| Room Agent 审批模式与确认详情 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/collab_app.go`、`desktop/collab_agent.go`、`desktop/frontend/src/collab`、协作测试 | “我的 Agent”复用普通 Session 的 `ask/auto/yolo` 工具审批模式，并直接修改/持久化同一 Session 策略；主人命令立即启动，后续工具调用遵循所选模式，自动响应任务仍仅在本次运行临时使用 scoped auto。Room 将 PendingInteraction 作为仅主人本机可见的结构化状态投影：工具审批复用 ApprovalModal，展示工具、对象、原因及允许一次/本会话/始终/拒绝；Ask 复用 AskCard，展示真实问题、选项、自定义答案并可直接回答，YOLO 也不会代答。详情不进入 Room 共享协议。Desktop Collaboration 专项、关键 race/vet、前端 142 项回归、TypeScript 与 Wails production 构建通过；验证版为 `desktop/build/bin/WorkGround2-room-agent-approval.exe`。直接处理，不使用 WorkGround2 Worker，已随本次版本合入主干。 |
| Room 主人命令授权边界 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/frontend/src/collab/useCollabController.ts`、`desktop/collab_agent.go`、协作测试 | 主人命令本身直接启动，不存在额外“开始任务”审批；工具权限由同一 Session 的 `ask/auto/yolo` 策略决定。自动识别、自动接受委托与被 @ 唤起等自动响应可对单次运行临时启用 scoped auto，完成后恢复原策略，且不会覆盖主人运行期间的主动修改。该语义由“Room Agent 审批模式与确认详情”调整并最终覆盖上一版硬编码主人命令 auto 的实现。 |
| Room @Agent 自动触发去重 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/frontend/src/collab/state.ts`、`desktop/frontend/src/collab/useCollabController.ts`、协作测试 | 修复带问号的显式 `@成员/@Agent` 消息同时命中 mention 扫描与自动回答问题扫描、在接收端产生两次运行的问题。自动问题扫描按结构化 `mentionMemberIds/mentionAgentIds` 跳过定向给本机的消息，由稳定请求 ID 的 mention 链路唯一处理；显式操作请求不受影响。前端 137 项协作回归、TypeScript 与 Wails production 构建通过；验证版为 `desktop/build/bin/WorkGround2-mention-dedupe.exe`。小型定向修复，不使用 WorkGround2 Worker，已随本次版本合入主干。 |
| Room Agent 等待确认决策 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/collab_app.go`、`desktop/collab_agent.go`、`desktop/frontend/src/collab/components/CollaborationTimeline.tsx`、`desktop/frontend/src/collab/useCollabController.ts`、协作测试 | `agent_command / waiting_approval` 卡片直接显示“同意/拒绝”，按 Session 与 Run ID 回答当前 Controller pending interaction；同意恢复原执行，拒绝工具审批也恢复原执行（结构化 Ask 的拒绝会取消对应 turn），均不调用 `StartCollaborationAgent`、不增加排队任务。等待卡隐藏会错误创建新任务的通用“同意并执行”菜单，重复点击幂等。Desktop 协作专项/race/vet、前端 135 项协作回归、TypeScript 与 Wails production 构建通过；验证版为 `desktop/build/bin/WorkGround2-agent-confirmation.exe`。小型定向修复，不使用 WorkGround2 Worker，已随本次版本合入主干。 |
| Room Agent 运行动态浮窗 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/frontend/src/collab/CollaborationWorkspace.tsx`、`desktop/frontend/src/collab/agentActivity.ts`、`desktop/frontend/src/collab/components/AgentActivityPopover.tsx`、`desktop/frontend/src/collab/collab.css`、协作测试 | 在线 Agent 为 running 时，鼠标悬停或键盘聚焦“运行中”会打开 Portal 浮层，按成员 ID 从 Room 权威 timeline 派生最近 6 条输入、运行摘要与输出，每 2.8 秒轮播；滚动、缩放、离开 running 自动关闭，reduced-motion 停止轮播。132 项协作回归、TypeScript、Vite 与 Wails production 构建通过；应用内 Browser 验证 `1/5 → 2/5` 实际轮播、视口内定位及 0 console error/warning，`design-qa.md` 为 passed。截图驱动 GUI 小改动，按规则不使用 WorkGround2 Worker；已随本次版本合入主干。 |
| Room Host Session 重启保留 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `internal/agent/save.go`、`internal/agent/save_test.go`、`desktop/collab_session_test.go` | 修复 Room 所属 Session 已持久化为 `collaboration`，却因 0 对话轮次被 Session 枚举过滤的问题。普通空白 Session 仍隐藏，collaboration Session 会按原 Workspace/topic/SessionPath 冷启动恢复到 Session List；覆盖数据层筛选与“未恢复原 tab”的桌面重启回归。`internal/agent` 全包、Desktop ProjectTree/Collaboration 定向测试、race、agent vet 与 diff check 通过。按用户要求不使用 WorkGround2 Worker，已随本次版本合入主干。 |
| Room 我的 Agent 任务队列 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/collab_app.go`、`desktop/collab_agent.go`、`desktop/collab_persist.go`、`internal/collab/store.go`、`desktop/frontend/src/collab`、对应测试 | Personal Agent 忙碌或 Controller 尚未就绪时，新委托和任务进入本机持久化 FIFO；最多等待 20 个，溢出显式拒绝且不会提前接受远端委托。队列在“我的 Agent”下展示，主人可幂等关闭等待项；统一就绪唤醒在任务结束、重连和重启恢复后自动拉起队首，重复唤醒不会重复启动，队首真正可运行前不会从可见队列移除。Room 状态按全部活跃 Run 投影，前一任务完成时仍有 queued Run 不再误显示空闲。Desktop 协作专项/race/vet、`internal/collab` 全包与专项 race/vet、diff check 通过；按用户要求不使用 WorkGround2 Worker，已随本次版本合入主干。 |
| 多人协作显式 Workspace 选择 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex + WorkGround2` | `desktop/frontend/src/App.tsx`、`desktop/frontend/src/collab`、协作测试 | 加入与创建 Room 共用始终可见的本地项目 Workspace 必选器；候选来自已登记项目，展示名称和完整路径，选中后用 generation guard 解析对应 blank Session，快速切换、清空选择及旧 Host/Join 迟到结果不会污染当前绑定，连接字符串导入不改本地 Workspace。协作回归 128/128、生产 TypeScript、CSS 语法及 z-index 检查通过。已随本次版本合入主干。 |
| Room 已接收文件操作 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/collab_file_transfer.go`、`desktop/frontend/src/collab`、对应测试 | 已接收成功的文件卡片增加默认“打开”和更多菜单“打开文件位置”；后端只按 Session/File ID 解析已完成接收记录中的权威路径，并校验普通文件后执行系统操作。文件传输专项、Desktop vet、协作回归 107/107、TypeScript、CSS、Vite 生产构建与底部菜单视觉验收通过。已随本次版本合入主干。 |
| Room 在线成员过滤与 @Agent | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `internal/collab`、`desktop/collab_app.go`、`desktop/collab_transport.go`、`desktop/frontend/src/collab`、对应测试 | 成员栏及计数只展示在线参与者；聊天输入按当前 Room 的在线成员和 Agent 匹配 @ 候选，排除自己的成员身份但保留自己的 Agent，Room 离线时仅显示自己的 Agent。mention 以成员/Agent ID 结构化持久化，目标成员或 Agent 被 @ 后不受自动识别开关影响，使用稳定请求 ID 至少启动一次本机 Agent 分析；失败显式显示并延迟重试，重复同步/重启由消息引用与命令 ID 去重。Internal Collaboration 全包/race/vet、Desktop Collaboration 专项/race/vet、前端 107 项协作回归、TypeScript、Vite 与独立 Wails 正式构建通过；验证版为 `desktop/build/bin/WorkGround2-collaboration-mentions.exe`。按用户要求不使用 WorkGround2 Worker。 |
| Room Agent 自动响应连续执行 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `desktop/collab_app.go`、`desktop/collab_agent.go`、`desktop/collab_persist.go`、`desktop/frontend/src/collab`、对应测试 | 修复“自动响应问题”只启动 Agent、工具调用仍等待人工确认的语义断层。自动识别问题、自动接受请求和被 @ 唤起的运行临时使用受策略约束的 `auto` 工具审批；显式拒绝和必须由用户决定的交互仍停下，手动运行保持会话原审批模式，运行结束后安全恢复，期间主人主动切换审批模式不会被覆盖。Desktop 定向测试、race、vet、前端 130 项协作回归、TypeScript、Vite 与 Wails 正式构建通过；验证版为 `desktop/build/bin/WorkGround2-collaboration-auto-response.exe`。Desktop 全量测试按用户要求中止。按用户要求不使用 WorkGround2 Worker。 |
| Room 客户端成员冲突恢复 | `done` | `developping/room-file-sharing+2026-08-04` | `Codex` | `internal/collab/errors.go`、`desktop/collab_app.go`、`desktop/collab_persist.go`、`desktop/collab_transport.go`、对应测试 | 恢复凭据按 Host/端口/Room/成员查询，不再绑定旧 Workspace，同一客户端可从新 Workspace 接管原成员；旧 Workspace 的失效连接会脱离且不能覆盖新凭据。没有凭据的其他客户端收到类型化 `resume_required` 后，切换为稳定的 Room/Session 作用域成员与 Agent ID 重试；兼容旧 Host 的同义 `unauthorized` 回包，token 等其他鉴权错误保持显式失败。Collaboration 后端、Desktop 专项与双方 race/vet、独立 Wails 正式构建通过；验证版为 `desktop/build/bin/WorkGround2-room-rejoin.exe`。按用户要求不使用 WorkGround2 Worker。 |
| Room 新消息智能贴底 | `done` | `developping/room-file-sharing+2026-08-04` | `Codex` | `desktop/frontend/src/collab/CollaborationWorkspace.tsx`、`desktop/frontend/src/lib/useScrollManager.ts`、协作测试 | Room 时间线复用统一 ScrollManager：首次进入自动贴底，用户位于底部时同步跟随新增及流式更新消息，主动上翻后保留阅读位置。生产 TypeScript、95 项协作回归、17 项滚动管理器契约、Vite 与独立 Wails 正式构建通过；验证版为 `desktop/build/bin/WorkGround2-room-autoscroll.exe`。小型 UI 修复，按用户要求不使用 WorkGround2 Worker。 |
| Room 右栏与个人 Agent 策略 | `done` | `developping/room-file-sharing+2026-08-04` | `Codex` | `internal/collab`、`desktop/collab_app.go`、`desktop/frontend/src/collab`、对应测试 | Room 右栏按 Windows 控制区高度留出安全空间；个人 Agent 配置置顶、成员列表下移；代号经幂等 `agent.update` 同步并支持离线 Outbox 与重启恢复，自动策略本地持久化，支持自动回答问题、自动接受操作请求，以及按消息/每 30 秒/关闭三种识别周期；自动响应标题独立成行，窄侧栏完整显示“问题 / 操作请求”两个开关；Timeline 的 Agent Run/结果按 Agent 身份映射代号和头像，不再复用成员名或“（你）”标记。Agent 身份回归与 Vite 正式构建通过；Collaboration 全套仅受并行 Relay 邀请对象新增 `version` 后的 2 个旧断言阻塞，TypeScript/Wails 受并行 Relay 页面及 Go 连接层未完成代码阻塞，因此现有验证版尚未包含本次 Timeline 名称修复。按用户要求不使用 WorkGround2 Worker。 |
| Room 文件分享与断点续传 | `done` | `developping/room-file-sharing+2026-08-04` | `Codex` | `internal/collab`、`desktop/collab_file_transfer.go`、`desktop/frontend/src/collab`、对应测试 | Room 任意成员可拖入文件形成共享卡片；文件内容由分享者本机提供，接收方显式选择保存位置，按 4 MiB 校验分块支持暂停、失败重试、断线及应用重启续传。接收端优先直连分享者，失败后经 Host 流式代理；Host 只持久化共享元数据，不保存文件内容。Collaboration 后端专项/race、Desktop 专项/race、双模块 vet、87 项前端回归、TypeScript、Vite 和 Wails 正式构建通过；全量 Go 测试因既有桌面长测持续无输出而终止。按用户要求不使用 WorkGround2 Worker。 |
| Room LLM 空响应安全降级 | `done` | `developping/collaboration-empty-intent-fallback+2026-08-04` | `Codex` | `internal/agent/semantic_intent.go`、对应测试 | 语义识别模型返回空白响应时直接缓存为普通聊天，不显示失败卡、不启动本机 Agent；非法非空协议值仍显式报错并允许重试。语义分类专项 race、Agent/Control vet 与独立 Wails 正式构建通过；验证版为 `desktop/build/bin/WorkGround2-collaboration-empty-fallback.exe`。小型后端调整，不使用 WorkGround2 Worker。 |
| 多人协作创建表单可用性 | `done` | `developping/collaboration-create-form-usability+2026-08-04` | `Codex` | `desktop/frontend/src/collab/components/ConnectionPanel.tsx`, `desktop/frontend/src/collab/collab.css`、协作测试 | 首次必填的本机身份保持展开，创建按钮不再因缺失字段静默禁用而改由原生表单指出具体必填项；连接卡片限制在视口内滚动，创建操作固定在表单底部可见，Room/身份字段使用宽屏双列，并收紧标题、标签页、间距和内边距。协作 74/74、生产 TypeScript、CSS、Vite 和独立 Wails 正式构建通过；验证版为 `desktop/build/bin/WorkGround2-collaboration-form.exe`。截图驱动 GUI 修复，按规则不使用 WorkGround2 Worker。 |
| Session 切换异步回收旧运行时 | `done` | `developping/session-open-runtime-close+2026-08-04` | `Codex` | `desktop/tabs.go`, `desktop/tabs_order_test.go` | single-surface 切换先完成快照、元数据落盘、可见注册表移除与持久化，再异步取消并关闭旧 Controller；供应商或 MCP 关闭迟滞不再阻塞 `ActivateTopic`，导航队列可继续响应后续点击。定向回归、race、Desktop vet 与独立 Wails 正式构建通过；修复版为 `desktop/build/bin/WorkGround2-session-open-fix.exe`。GUI 回归，按规则不使用 WorkGround2 Worker。 |
| Room LLM 语义意图回退 | `done` | `developping/collaboration-llm-intent-fallback+2026-08-04` | `Codex` | `internal/agent/semantic_intent.go`, `internal/control/semantic_intent.go`, `desktop/collab_app.go`, `desktop/frontend/src/collab`、对应测试 | 正则分类明确命中时保持原路径，默认未覆盖项异步调用当前 Session 模型输出 `self_agent/uncertain/chat`；不改 Session transcript 与缓存前缀，同文案缓存、并发合并，迟到结果按消息 revision 拒绝。超时、失败及非法输出显式显示为可手动开始/停止的失败提示，消息发送不受阻塞。Agent/Control 包测试与 vet、全仓 vet、Desktop 协作专项与 vet、语义分类 race、前端协作 69/69、生产 TypeScript、Vite 与 Wails 正式构建通过；测试 TypeScript 全量仍受既有 Iris/Project Tree/Run Events/Work fixture 基线错误阻塞，主模块全量测试仍命中既有 `internal/work` Definition revision、WorkView golden、Cornerstone projection 等失败。验证版：`desktop/build/bin/WorkGround2-collaboration-intent.exe`。WorkGround2 Desktop 不可达，Codex 本地实现。 |
| 协作 Agent 启动期排队 | `done` | `developping/collaboration-agent-start-queue+2026-08-03` | `Codex` | `desktop/collab_app.go`, `desktop/frontend/src/collab`、对应测试 | Session Controller 尚在启动时，倒计时请求先幂等登记为 queued，Controller 就绪后自动发布 running 并提交本机 Agent；启动最终失败显式暴露，失败状态的“立即开始”可真正重新请求。当前现场的待同步聊天未留下失败 Run 引用，启动新版后仍会由 Outbox 回放倒计时。Go 协作专项、竞态测试、Desktop vet、前端协作 62/62、TypeScript、Vite 和 Wails 正式构建通过；验证版：`desktop/build/bin/WorkGround2-collaboration-agent-start.exe`。沿用用户要求，不使用 WorkGround2 Worker。 |
| 协作 Outbox Agent 意图回放 | `done` | `developping/collaboration-pending-intent-replay+2026-08-03` | `Codex` | `desktop/frontend/src/collab/state.ts`, `desktop/frontend/src/collab/useCollabController.ts`、对应测试 | 旧版本已排队但尚未生成倒计时的本机消息会在恢复时重新识别；若 Outbox 中已有引用该消息的 Agent Run 则不重复介入。前端协作 60/60、TypeScript 与 Wails 正式构建通过，验证版沿用 `desktop/build/bin/WorkGround2-collaboration-offline-agent.exe`。沿用用户要求，不使用 WorkGround2 Worker。 |
| 多人协作 Host 恢复与本机 Agent 介入 | `done` | `developping/collaboration-host-recovery-agent-intent+2026-08-03` | `Codex` | `desktop/collab_app.go`, `desktop/collab_persist.go`, `desktop/frontend/src/collab/state.ts`、对应测试 | 恢复元数据已与实时连接解耦，断线写 Outbox 不再清空身份/凭据引用；旧的残缺 Host 记录会从成员快照、所属 Session 和确定性凭据引用自愈。提交/commit/build/test 等本机指令会进入可停止倒计时，只有自己且 Room 离线时仍可运行本地 Agent，运行/结果继续排队同步。现场缓存确认正好缺失 memberName/agentName/connectionSecretRef，成员快照与对应凭据仍完整可恢复。Desktop Collaboration 专项与 vet、前端 58/58、TypeScript、Vite 和 Wails 正式构建通过；验证版：`desktop/build/bin/WorkGround2-collaboration-offline-agent.exe`。沿用用户要求，不使用 WorkGround2 Worker。 |
| 多人协作会话可靠性 | `done` | `developping/collaboration-session-reliability+2026-08-03` | `Codex` | `desktop/collab_*`, `desktop/frontend/src/collab`, `desktop/frontend/src/App.tsx`, `internal/collab/model.go`、对应测试 | 协作标题栏已成为 Wails 拖拽区且交互控件保持可点；Host 自身直连本地权威 Room Service，不再因回环 HTTP/SSE 抖动误报离线。离线发送会由持久 Outbox 立即投影为待同步消息，重连后由正式权威消息无重复替换。输入区复用全局 Enter/Ctrl+Enter 配置、IME 保护和当前 Session 模型选择。Internal Collab、Desktop Collaboration 专项与 vet、前端 53/53、TypeScript、Vite 生产构建和 Wails 正式构建通过；验证版：`desktop/build/bin/WorkGround2-collaboration-reliability.exe`。沿用用户要求，不使用 WorkGround2 Worker。 |
| 协作 Agent 忙状态容错 | `done` | `developping/collaboration-agent-busy+2026-08-03` | `Codex` | `desktop/collab_app.go`, `desktop/frontend/src/collab`、对应测试 | 重复启动由前端 in-flight 闩锁合并，Agent 运行期间禁用执行类入口和自动倒计时；后端用结构化 `agent_busy` 返回保留单运行约束。业务拒绝显示为非致命操作提示，不再把 Room 改成连接失败，所有 UI Promise 边界均显式消费，避免 `unhandledrejection`。协作 Go 专项、Desktop vet、TypeScript、前端生产构建、37 项协作测试与 Wails 正式构建通过。验证版：`desktop/build/bin/WorkGround2-agent-busy-fix.exe`。沿用用户要求，不使用 WorkGround2 Worker。 |
| 多人多 Agent 协作 | `done` | `developping/collaboration-multi-runtime+2026-08-03` | `Codex` | `desktop/collab_app.go`, `desktop/collab_persist.go`, `desktop/collab_transport.go`, `desktop/frontend/src/collab`, `docs/MULTI_CLIENT_AGENT_COLLABORATION.zh-CN.md`、对应测试 | App 级单例已改为 `sessionId → CollaborationRuntime`：每条协作 Session 独立连接 Room，并隔离心跳、重连、共享背景缓存、Outbox、Agent Run、事件和持久化；多个 Room 可同时在后台保持连接。Host 与加入者断线/重启后保留完整远端快照，本地 Agent 可继续引用缓存执行，更新按原 Room 幂等补发。协作 Go 专项、子包测试、Desktop vet、前端生产构建、31 项协作测试和 Wails 正式构建通过；Desktop 全量 Go 与协作 race 分别在 6/2 分钟上限内无失败输出但未完成。验证版：`desktop/build/bin/WorkGround2-multi-room.exe`。沿用用户要求，不使用 WorkGround2 Worker。 |
| Session 滚动条拖动保护 | `done` | `developping/work-restart-choice+2026-08-02` | `Codex` | `desktop/frontend/src/lib/useScrollManager.ts`, `desktop/frontend/src/components/Transcript.tsx`, `desktop/frontend/src/__tests__/scroll-manager-contract.test.ts` | 鼠标按住会话纵向滚动条时，暂停新问题、流式内容、尺寸变化和延迟布局触发的自动跟底，并取消已排队的回贴；松开后依据当前位置重新计算跟随状态，离开底部继续保留阅读位置。专项 17/17、生产 TypeScript、CSS 检查与 Vite 构建通过；测试 TypeScript 全量仍命中当前工作树既有 fixture/type 错误。按用户要求不使用 WorkGround2 Worker。 |
| Desktop 异步派发握手 | `done` | `developping/work-restart-choice+2026-08-02` | `Codex` | `desktop/remote_api.go`, `desktop/tabs.go`, `desktop/app.go`, `desktop/ai_collaboration_skill`、对应测试 | `/session/new` 创建并登记后立即返回可查询的 `starting` SessionID；初始任务在 Controller 启动期持久排队，同内容重复提交幂等、冲突内容返回 409，启动成功自动重放，崩溃恢复先对账历史避免重复执行，失败状态保留 ID、任务和显式错误。派发脚本立即返回 `dispatched`，`PollOnly` 改为单次有界快照；内嵌及已安装 Skill 升至 1.1.0/v2。相关 API/Skill/PowerShell 专项、race、Desktop vet、旁路 EXE 构建和 Skill 校验通过。Desktop 全包 275 秒完成，仍有既有 Darwin 路径、临时 Session 清理及 Work V2 Wails 基线失败；本次协议断言已修复并通过。按用户要求不使用 WorkGround2 Worker。 |
| Work 重新开始选择与流程复用 | `done` | `developping/work-restart-choice+2026-08-02` | `Codex` | `internal/work`, `desktop/works.go`, `desktop/frontend/src/components/work`、对应样式与测试 | 现有圆箭头统一为“重新开始”：保留播放/暂停/停止/重启四个控制，点击后选择“重启当前运行”或“保存流程并再次运行”；后者首次冻结工作结构、工具与输出格式，只开放选定变量，之后直接填写变量并在独立 Work Session 启动。流程保存和再次运行均幂等、失败可重试且不改原 Work。后端流程/重启专项、Desktop 编译、前端弹窗与控制条 28 项、TypeScript、CSS、Vite/Wails 生产构建及真实窗口对照验收通过。按用户要求不使用 WorkGround2 Worker。 |
| Work 信息完成后倒计时启动 | `done` | `developping/work-info-auto-start+2026-08-02` | `Codex` | `internal/work/input_service.go`, `internal/work/v2_coordinator.go`, `desktop/frontend/src/work/components/presentation/WorkAutoStartCountdown.tsx`, `desktop/frontend/src/work/components/presentation/WorkInformationPanel.tsx`、对应样式与测试 | 工作信息全部填写后在原信息标题行显示 20 秒倒计时；打开信息自动暂停，也可手动暂停、继续或立即开始。最终信息先持久化为 `readyForStart` 草稿，全部释放后统一恢复调度，重启不会绕过等待，多次启动可幂等重放。后端恢复/调度专项、前端 47 项组件测试、TypeScript、CSS、生产构建、Desktop 编译/vet，以及 Carbon 主题与 1440/1024/600 宽度视觉 QA 通过；按用户要求不使用 WorkGround2 Worker。 |
| Work 未物化任务调整恢复 | `done` | `developping/work-pending-patch-task+2026-08-01` | `Codex + WorkGround2` | `internal/work/discussion_block.go`, `internal/work/patch_service.go`, `internal/work/patch_service_test.go` | 完整 DAG 预显的 pending 节点尚无 V2TaskRuntime 时，PreviewWorkPatch 现在只接受当前 Run 与活动 Definition 可精确推导的稳定 Task ID，并在 Apply 阶段再次校验节点与 Block 绑定；伪造、跨 Run、缺失节点继续显式失败且不产生副作用。pending workflow Preview→Apply→幂等回放、Block 路径、身份拒绝、既有 Patch 核心专项及 `go vet ./internal/work` 通过；全部 TestPatch 扩大回归仍命中自动 revision 重排后 3 个既有故障注入断言。 |
| Work 信息保存后自动前进 | `done` | `developping/work-info-auto-advance+2026-08-01` | `Codex` | `desktop/frontend/src/work/components/presentation/WorkInformationPanel.tsx`、对应测试 | “保存并继续”同时监听提交回执和权威 pending 队列变化：投影先完成时自动切到下一条，最后一项或“填完后先关闭”才关闭；面板已关闭/切换时不被迟到回执覆盖。面板专项 36/36、TypeScript、前端生产构建通过；Work 套件中本面板及上下游通过，整套仍命中已记录的 WorkCard/Cornerstone 旧基线。小范围交互修复，Codex 本地完成。 |
| Work 版本冲突自动收敛 | `done` | `developping/work-revision-auto-retry+2026-08-01` | `Codex（WorkGround2 Desktop 不可用后本地恢复）` | `internal/work/store_file.go`, `internal/work/input_service.go`, `internal/work/definition_service.go`, `internal/work/patch_service.go`, `internal/work/artifact_slot.go`, `internal/work/cornerstone_manager.go`, `internal/work/block_service.go`、对应测试 | `CommitEvent(s)` 在持有权威写锁后自动按最新 Work revision 重排事件链，批次保持原子性，重复 requestId 返回原 revision，receipt 同步实际 revision；整份草稿写入和对象级 revision/definition/content 冲突继续显式返回，避免覆盖并发业务修改。AddCustomInput 的 baseRevision=74/75 竞态及单事件、批次、重复提交、并发块/Cornerstone 回归通过；`go vet ./internal/work` 通过。全包验证另有既有 V2 fixture、Boot 文档/配置迁移、Serve 语言偏好失败。 |
| Work 启动草稿幂等恢复 | `done` | `developping/work-draft-intent-idempotency+2026-08-01` | `Codex` | `internal/work/service.go`, `internal/work/events.go`, `desktop/frontend/src/components/work/WorkCard.tsx`、对应测试 | `start/draft` 以调用方字段摘要判定同一意图，忽略 expectedRevision、状态与自动标题漂移；重复请求读取不可变原事件并返回最新投影，压缩后仍保留恢复凭据，也兼容无摘要旧事件。初始启动沿用旧 requestId 以恢复现场，用户修改提示或标题后使用内容摘要生成新的稳定 requestId。UpdateDraft/压缩专项、Controller 12 场景、Go vet、TypeScript 与前端生产构建通过；WorkCard 全量脚本已通过本次新增断言，后续仍命中既有界面断言与空节点异常，Work 包全量在 180 秒门限内未完成。按用户要求不使用 WorkGround2 Worker。 |
| Work 载入等待态重新设计 | `done` | `developping/work-loading-state+2026-08-01` | `Codex` | `desktop/frontend/src/components/work/WorkCard.tsx`, `desktop/frontend/src/styles.css`, 对应测试 | 将正常后台同步与错误恢复态分离；使用清晰的载入说明、轨道/呼吸/进度动画、弱化诊断 ID、窄窗布局和 reduced-motion 降级。App 集成契约 37/37、CSS、TypeScript、Vite 生产构建、1280px/460px 视觉验收和 Wails 正式构建通过。验证版：`desktop/build/bin/WorkGround2-loading-redesign.exe`；截图中的 13:13 旧实例需退出后启动新版才能加载新嵌入资源。按用户要求不使用 WorkGround2 Worker。 |
| Work 工作信息详细生成与自行推断 | `done` | `developping/work-info-inference+2026-08-01` | `Codex` | `internal/boot/definition_planner.go`, `internal/boot/input_inferrer.go`, `internal/work/input_inference.go`, `desktop/works.go`, `desktop/frontend/src/work` | Definition Planner 会拆分互相独立的用户决策与约束，并为每项生成更具体的问题和用途说明；工作信息标题栏增加“自己推断”，模型按 InputSpec/Schema 生成可审阅草稿，文件、授权及缺少可靠依据的项显式跳过，用户确认后仍走既有幂等提交与 revision 恢复链路。后端推断/提示专项、Controller/Desktop vet 与绑定契约、Panel 23 项、Overview 9 项、TypeScript、CSS、前端生产构建、Wails 正式构建及本地交互视觉验收通过；Work `-short` 宽回归仍命中 Feature Map 已记录的状态迁移、Blueprint 数量、Secret detector 与 Form number 基线。截图驱动 GUI 任务，按规则未使用 WorkGround2 Worker。 |
| Session 发送为工作入口稳定显示 | `done` | `developping/work-send-entry-stability+2026-08-01` | `Codex` | `desktop/frontend/src/App.tsx`, 对应测试 | 入口可见性只由普通可写 Session 的真实用户首条消息、后端 blank 和 Work 过渡状态决定；内部 Item、历史恢复、WorkEnabled/WorkCapable 异步结果不再撤销入口。后端探测只传 Tab ID，缺失时用空 ID 活动路由；配置与能力在点击时显式校验。Work 合约 35 项、TypeScript、Wails 正式构建通过，正式 EXE 在启动 12 秒后及追加 15 秒延时验收中均持续显示。按用户要求不使用 WorkGround2 Worker。 |
| Session 发送为工作入口运行时修复 | `done` | `developping/work-send-entry-runtime+2026-08-01` | `Codex` | `desktop/frontend/src/App.tsx`, `desktop/frontend/src/components/SessionSurface.tsx`, `desktop/frontend/src/components/desktop-ui/RuntimeConfigBar.tsx`, `desktop/tabs.go`, 对应测试 | 根因是工作台隐藏了 Composer 自带元数据栏，入口此前渲染在不可见区域；现由实际可见的 RuntimeConfigBar 承载并复用同一发送状态。恢复会话可用 Session ID 或后端活动 Tab 路由，空白检测读取 Controller 当前路径，仍由 CreateWorkSession 做最终原子校验。Work 合约 34 项、TypeScript、CSS、Go 定向测试、Wails 正式构建及运行窗口可访问性/选中态验收通过。按用户要求不使用 WorkGround2 Worker。 |
| Session 发送为工作入口常驻 | `done` | `developping/work-send-entry-visibility+2026-08-01` | `Codex` | `desktop/frontend/src/App.tsx`, `desktop/frontend/src/__tests__/app-work-integration.test.tsx` | 空白普通 Session 的入口由后端 blank 状态决定，不再因异步 Work 能力或 Topic 轮次探测而消失；选择时再确认能力，失败显式提示且不转换 Session。Work 集成契约 31 项、TypeScript、CSS、前端生产构建和 Wails 正式构建通过。按用户要求不使用 WorkGround2 Worker。 |
| Work 后新建 Session 回归修复 | `done` | `developping/session-new-session-regression+2026-08-01` | `Codex` | `desktop/tabs.go`, `desktop/app_session_dedup_test.go` | Work Session 不再被空白 Session 复用逻辑误判，Work 元数据也不再标记为空白；新建会话会创建独立普通 Session。相关会话去重测试、Go vet、Diff 检查和 Wails 正式构建通过。按用户要求不使用 WorkGround2 Worker。 |
| Session 首条消息发送为工作 | `done` | `developping/session-work-mode+2026-08-01` | `Codex` | `desktop/works.go`, `desktop/frontend/src/App.tsx`, `desktop/frontend/src/components/Composer.tsx`, `desktop/frontend/src/components/work/WorkSessionTransition.tsx`, 对应测试 | 已移除独立 Work 创建首屏和 App 的独立新建入口；空白普通 Session 可将首条消息发送为工作并原地绑定同一 tab。结构规划期间保持 Session 视觉，Definition 应用后以独立展开动画显现现有 WorkCard；初始化失败可重试，结构澄清/生成失败会显现 Work 面板继续处理。原地转换/幂等恢复 Go 专项、App 29 项、过渡 9 项、发送选项 3 项、TypeScript、CSS、生产构建和 Desktop vet 通过；WorkCard 宽回归仍命中 Feature Map 已记录的初始背面、ResultShelf 无障碍和成果重试空节点基线。按用户要求不使用 WorkGround2 Worker。 |
| Work 关联会话直接打开 | `done` | `developping/work-linked-session-direct-open+2026-07-31` | `Codex` | `desktop/frontend/src/App.tsx`, `desktop/frontend/src/components/work/LinkedSessionCard.tsx`, 对应测试 | 工作结构任务的信息按钮翻到背面后自动打开目标 SessionRef；隐藏的 Work Task 会话优先复用所属 Work tab/topic，失败保留显式错误与可重试入口。自动打开/失败重试 6 项、App 路由契约 28 项、TypeScript 与生产前端构建通过；WorkCard 宽回归仍命中 Feature Map 已记录的翻面、ResultShelf 可访问性与成果重试空节点基线。 |
| Work recovery 会话来源恢复 | `done` | `developping/work-session-source-label+2026-07-31` | `Codex + WorkGround2` | `internal/agent/save.go`, `internal/agent/save_test.go`, `desktop/tabs_work_task_test.go` | 根因是 Work Task 已有 `work:` 来源，但 snapshot conflict 创建 recovery branch 时未继承，旧 Desktop 迁移随后标成 `external` 并暴露到 SessionList。新 recovery branch 会继承父来源；旧 branch 在列表读取时沿 `parent_id` 幂等恢复，继续由既有 Work Task 过滤隐藏，真实 external/CLI/普通 Desktop 保持不变。Agent 全包、Desktop 定向回归及两模块 vet 通过；Desktop 全包仍命中已记录的 Darwin 路径、Topic 切换/清理与 Work Watch/Session binding 基线。 |
| Work Shell 内嵌脚本成果发现 | `done` | `developping/work-info-entry-panel+2026-07-30` | `Codex + WorkGround2` | `internal/artifact/producer_file.go`, `internal/artifact/artifact_test.go` | 修复 Task 通过 Shell 内嵌 Python `doc.save('路线指引.docx')` 等明确输出 API 生成文件后，因终端乱码且无 `-o/--output` 而被 Artifact Collector 漏收、成果槽误报 0 个匹配的问题。仅解析 `.save/.savefig/.to_excel/.to_csv/.to_json/.to_parquet` 的首个字符串字面量参数；普通读取、echo 提及、变量表达式不进入成果，真实文件仍经既有路径、文件类型、大小与 Blob 物化校验。Artifact 全包、TaskExecutor/TaskArtifact 专项及 Artifact vet 通过。 |
| Work 对话注入权威上下文 | `done` | `developping/work-info-entry-panel+2026-07-30` | `Codex + WorkGround2` | `internal/control/work_context.go`, `internal/control/controller.go`, `desktop/work_chat.go`, `desktop/frontend/src/App.tsx`, `desktop/frontend/src/lib/bridge.ts`, 对应测试 | Work 正面底部聊天发送前由 Desktop 后端校验 tab/work 绑定并读取权威 WorkView；当轮模型输入包含 Work/Definition、运行失败与等待、输入、成果和阻断摘要，UI 与历史仅展示用户原文。上下文按失败信息优先、稳定排序、UTF-8 安全方式限制在 8000 字节；普通会话、命令、steer 与 deferred rewind 继续复用原发送流程。Controller/Desktop 专项、WorkChatInput 18 项、相关 vet、TypeScript 与生产前端构建通过。 |
| Work 完成后信息变更重算 | `done` | `developping/work-info-entry-panel+2026-07-30` | `Codex` | `internal/work/input_service.go`, `internal/work/store_file.go`, `internal/work/v2_coordinator.go`, `desktop/frontend/src/work/components/presentation/WorkInformationPanel.tsx`, `WorkControlBar.tsx`, 对应测试 | 已完成 Work 的既有输入发生实际变化时，输入提交事件原子失效目标节点与已物化下游，重置对应成果并自动重算；相同内容安全保存且不重跑。编辑抽屉在权威输入修订到达时立即收起，不等待同步重算结束；完成态禁用会误走空 legacy Workflow 的“开始”，保留“重启”。Go 完成态重算/运行中迟到结果/影响范围专项、Panel 20 项、控制栏 17 项、TypeScript、生产前端构建、Go vet 与 diff check 通过；完整前端 Work 回归 64 秒内未结束，较宽 Go 输入回归仅命中共享脏工作树中已记录的兼容输入跨 Definition 迁移与旧隔离用例冲突。验证版：`desktop/build/bin/WorkGround2-input-rerun-fix.exe`。 |
| Work 独立填写信息面板 | `done` | `developping/work-info-entry-panel+2026-07-30` | `Codex` | `desktop/frontend/src/components/work/WorkCardFront.tsx`, `desktop/frontend/src/work/components/presentation/WorkDefinitionOverview.tsx`, `desktop/frontend/src/work/components/presentation/WorkInformationPanel.tsx`, `desktop/frontend/src/work/controller.ts`, `desktop/frontend/src/work/wailsAdapter.ts`, `internal/work`, `desktop/works.go`, 对应测试 | 已填写项可重新打开修改；用户可新增名称、可选解释、文本或文件内容。自定义项复用带内联规格的 WorkInput，以双事件批次原子落盘并作为 Work 级上下文供后续任务读取；文件支持点击、拖放和移除。Panel 18 项、后端原子/编辑/摘要专项、DTO 契约、全仓 Go 编译、TypeScript、契约检查、前端生产构建与 Wails 正式构建通过；浏览器插件本轮只暴露标签页能力，未声称截图式视觉验收。按用户要求不使用 WorkGround2 Worker。EXE：`desktop/build/bin/WorkGround2.exe`。 |
| Work 成果重试 revision 冲突自愈 | `done` | `developping/work-artifact-retry-conflict-recovery+2026-07-31` | `Codex + WorkGround2` | `internal/work/collaboration_recovery.go`, `internal/work/collaboration_recovery_test.go` | 现场 Work `work-4fd3ae8ca302a186aafd8ac0` 在成果已进入 `generating` 后，重复重试仍连续写入 693→696 三条 no-op 事件；并发请求耗尽固定三次 rebase 后暴露 `expected revision 693, current revision 696`。现 generating/ready 重试直接返回权威槽与 Work revision，不写事件/receipt、不调度生产者；相同及 8 路不同 requestId 并发均幂等成功且 revision 不变，真实失败槽仍走原持久化回执与调度路径。新增用例连续 5 轮、既有成功路径、Work 包 vet、全仓 vet 与 diff check 通过；RetryArtifactSlot 宽回归的历史 revision/Definition 切换 3 项失败已在未修改 main 基线复现，全仓测试仍命中既有 Boot/CLI/InstallSource/Serve 基线并在 Work 长尾 5.3 分钟无输出后停止。 |
| Work DOCX 成果采集恢复 | `done` | `developping/work-docx-artifact-collection+2026-07-31` | `Codex + WorkGround2` | `internal/artifact/producer_file.go`, `internal/artifact/artifact_test.go` | FileProducer 会去除 shell 结果 ANSI SGR、逐行解析带引号 token，并仅按明确成果扩展名白名单接纳相对文件；`路线指引.docx saved successfully.`、PowerShell ANSI `Get-Item` 表格及带空格引号路径均可落入既有工作区安全校验，版本号、域名、未知后缀、源码、URL、失败结果不误采。Artifact 44 项、Control 全包和全仓 vet 通过；全仓 test 仍命中 Feature Map 已记录的 Boot 工具契约/旧 tier、CLI、InstallSource、Serve 基线，并在 Work 长尾约 5 分钟无新增输出后停止。 |
| Work 结构直接承载运行状态 | `done` | `developping/work-remove-duplicate-structure+2026-07-30` | `Codex` | `desktop/frontend/src/components/work/WorkCardFront.tsx`, `desktop/frontend/src/work/components/presentation/WorkDefinitionOverview.tsx`, `desktop/frontend/src/work/components/v2/ExecutionList.tsx`, 对应测试 | 合并前台重复层级：保留一个默认展开的“工作结构”区域，结构节点直接显示运行状态、进度、结果摘要并承载讨论/展开操作；移除独立“执行任务”和内层“AI 正在执行”标题，工作信息继续独立展示。ExecutionList 252 项、Definition Overview 9 项、TypeScript、CSS、生产构建与 Diff 检查通过；WorkCard 新增结构合并断言通过，宽回归仍命中 Feature Map 已记录的自动应用失败态、ResultShelf 无障碍及默认 Wails 输入挂载基线。按用户要求未使用 WorkGround2 Worker。 |
| Work LLM 语义影响决策 | `done` | `developping/work-llm-impact-decision+2026-07-30` | `Codex` | `internal/boot/patch_planner.go`, `internal/work/patch_service.go`, `internal/work/patch_actions.go`, `internal/control/task_artifact.go`, 前端契约与对应测试 | Patch Planner LLM 同时声明 `reuse/reformat/rerun/ask_user` 语义动作；确定性层只校验引用、转换前提与幂等边界，并严格按该决策调度。单纯成果格式变化复用已完成节点、用户输入和搜索证据，通过本地成果转换生成新格式，不启动模型、不重复 web search；结构恢复可安全重放且不重复转换。Patch Planner、成果转换、补丁/协调器、Wails 动作透传专项，TypeScript、相关 vet 与 diff check 通过。按用户要求未使用 WorkGround2 Worker。 |
| Work 用户调整自动协调 | `done` | `developping/work-auto-coordinator+2026-07-30` | `Codex` | `internal/work/runtime_reuse.go`, `desktop/frontend/src/components/work/WorkCardBack.tsx`, `desktop/frontend/src/work/controller.ts`, `desktop/frontend/src/work/components/v2`, 对应测试 | 用户修改目标、参数、结构或成果要求后，兼容且无歧义的输入跨 Definition 修订迁移；LLM Patch/Definition 规划、应用、权威刷新和受影响节点续跑自动串联，revision/digest 漂移会刷新后有界重基，瞬时失败按同一幂等请求自动恢复。讨论与成果界面仅保留“提交修改”，不再展示预览、确认应用或技术重试；只有输入不兼容、存在歧义或自动恢复耗尽时才请求用户补充。后端迁移专项、Controller、Discussion、ExecutionList、ResultShelf、TypeScript 与生产构建通过；Work 宽回归仍命中 Feature Map 已记录的 ResultShelf 无障碍、Cornerstone Attention/Watch 时序基线。 |
| Work 候选结构迟到 revision 自愈 | `done` | `codex/work-general-model-ui-2026-07-30` | `Codex` | `desktop/frontend/src/work/controller.ts`, `desktop/frontend/src/work/store.test.ts` | 现场事件日志确认候选结构已在 Work revision 514 落盘，前端投影仍为 513，原固定四次刷新门禁将真实成功误报为失败。提交回执包含完整 Candidate 与权威 revision 时，候选确认和应用直接沿回执继续，事件流负责最终对账；只有缺少 Candidate 的不完整回执继续执行严格权威恢复。Work Store 88 项、TypeScript、完整前端生产构建、Diff 检查和 Wails 正式构建通过。 |
| Work 真实数据视觉基线收敛 | `done` | `codex/work-general-model-ui-2026-07-30` | `Codex` | `desktop/frontend/src/components/work/WorkCardFront.tsx`, `desktop/frontend/src/work/components/presentation`, `desktop/frontend/src/styles.css`, `design-qa.md` | 以生产 Definition 3 的 2 个 reserved 成果、1 个 waiting_input 任务、6 个 requested 输入和自动节点摘要 Block 复核首次输入态；等待状态补齐第三段信息摘要，未修改且纯重复的自动节点摘要隐藏，无真实业务 Block 时由 Definition/Input 通用投影“工作信息 + 工作结构”，真实 Block 到达后自然替换，未枚举工作类型。1536×1024 同输入视觉对照、执行摘要展开/折叠、控制台 0 error、新增组件 33 项、TypeScript、CSS、生产前端和 Wails 正式构建通过；WorkCard 宽回归仍只命中 Feature Map 已记录的 ResultShelf 无障碍选择器及默认 Wails 输入挂载基线。EXE：`desktop/build/bin/WorkGround2.exe`。 |
| Work 补丁提交假失败自愈 | `done` | `codex/work-general-model-ui-2026-07-30` | `Codex` | `desktop/frontend/src/work/controller.ts`, `desktop/frontend/src/work/store.test.ts` | ApplyWorkPatch 首次响应无法确认且没有明确业务拒绝时，先刷新权威快照，再以同一 requestId 有界重放一次；已提交请求通过幂等回执恢复成功，明确 revision/request 冲突只刷新且继续显式失败。本次现场权威事件确认补丁已在 Work revision 394 提交、Definition 2→3 且新 Run 已进入 waiting，原提示属于假失败。Work Store 87 项、TypeScript 检查、完整前端生产构建与 Wails 正式构建通过；Work 总回归中本次 Store/ExecutionList/ResultShelf/discussion 等专项通过，仍命中 Feature Map 已记录的 ResultShelf 无障碍选择器、Cornerstone Attention 与 Watch 健康时序基线失败。首次 Wails 构建受 pnpm 非 TTY 确认拦截，设置标准 CI 模式后成功恢复。 |
| Codex CLI 搜索能力探测恢复 | `done` | `codex/work-general-model-ui-2026-07-30` | `Codex` | `internal/config/cli_capability.go`, `desktop/onboarding_cli.go`, `docs/GUIDE.zh-CN.md`, `docs/SPEC.md`, 对应测试 | 带 `--ignore-user-config` 的 Codex provider 会在临时空 `CODEX_HOME` 下探测能力，隔离与普通探测使用不同缓存，避免用户级 Codex 配置暂时不兼容时误判为无 `web_search`；桌面扫描传入真实 Provider 参数。已移除本机无效的 `service_tier = "default"`，真实 Codex JSONL 请求已产生 `web_search` 事件并返回官方 URL。Config 全包、Control 搜索门禁专项、Desktop CLI 扫描专项、相关 vet、diff check 与 Wails 正式构建通过；Desktop 全包仍命中既有 Darwin 路径、Topic 树及 Work Session 异步基线失败。 |
| Work 通用模型与状态化工作台 | `done` | `codex/work-general-model-ui-2026-07-30` | `Codex + subagent` | `internal/work/artifact_slot.go`, `internal/work/collaboration_recovery.go`, `desktop/frontend/src/work/presentation.ts`, `desktop/frontend/src/work/components/presentation`, `desktop/frontend/src/components/work/WorkCardFront.tsx`, `docs/WORK_GENERAL_MODEL_AND_UI.zh-CN.md`, `design-qa.md` | 以 Definition、Task、Block、Artifact 的领域无关投影驱动 planning/running/waiting/failed/completed 五态和结构/均衡/关注/结果四种布局；Block 使用真实 placement span，成果架、状态面板、执行摘要与目标形成稳定骨架。成果重试仅对目标对象契约做严格校验，聚合 revision 漂移有界自动重基，重复请求幂等回放。团建、财务、软件工程仅作视觉覆盖样本，生产代码无领域枚举。前端 803 项专项、类型检查、生产构建和浏览器三态/720px 视觉 QA 通过；后端专项通过，宽回归保留既有 Work 基线失败。按用户要求使用 subagent，未调用 WorkGround2 Worker。 |
| Work 失败任务成果状态同步 | `done` | `developping/work-failed-artifact-status+2026-07-30` | `Codex` | `desktop/frontend/src/components/work/WorkCard.tsx`, `WorkCardFront.tsx`, `desktop/frontend/src/work/components/v2/ResultShelf.tsx`, 对应测试 | 修复生产任务已为 `failed_retryable`、关联成果仍显示“生成中”的状态错位；成果架从活动 Definition 的产出关系与当前 Run Task 状态派生即时展示，失败时提供可重试状态，任务重试运行后恢复生成态。ResultShelf 257 项、TypeScript 与 Vite 生产构建通过；WorkCard 全套仍命中 Feature Map 已记录的 ResultShelf 无障碍选择器及默认 Wails 输入提交基线。截图驱动的局部 UI/状态映射修复，按规则不使用 WorkGround2 Worker。 |
| Work 搜索硬能力自动预取 | `done` | `developping/work-web-search-capability-preflight+2026-07-30` | `Codex + WorkGround2` | `internal/control/taskexec.go`, `internal/control/taskexec_test.go` | 节点要求 `web_search`、执行模型没有原生搜索能力且无直接搜索工具时，TaskExecutor 会在主模型执行前经标准 `request_help(web_search)` 路径幂等预取，成功/失败均进入同一 Task Session；成功回执由既有完成门禁识别，失败保留给主模型回退并最终显式 `failed_retryable`。原生搜索、直接搜索工具不抢占，重复要求去重，`image_generation` 与未来能力不会误入通用预取，`web_fetch` 加 URL 仍不算搜索证据。Control 全包、Work 搜索门禁专项、全仓 vet、根 CLI build 与 diff check 通过；全量测试 184 秒无输出超时，`-short` 门禁仅命中 Feature Map 已记录的 Work Blueprint/DTO/ArtifactSlot/secret detector 与 Serve 基线失败。 |
| Work 已完成 Block 标题可读布局 | `done` | `developping/completed-block-title-layout+2026-07-29` | `Codex` | `desktop/frontend/src/styles.css`, `desktop/frontend/src/work/components/v2/ExecutionList.test.tsx` | 修复执行列表标题被长进度文本压缩到只剩一两个字；标题保留响应式不可压缩宽度，右侧进度摘要优先截断，操作按钮保持可用。已通过 254 项组件测试、TypeScript 检查、Wails 构建及实际界面验收。 |
| Work 实时进度持久化性能优化 | `done` | `developping/work-live-persistence-performance+2026-07-29` | `Codex` | `internal/work/events.go`, `internal/work/store_file.go`, `desktop/frontend/src/components/work/WorkRunEntry.tsx`, 对应测试 | Task live update 稳定态改为校验 projection/manifest/index/log tail 后增量归约，不再逐次全量回放；request receipt 拆为追加式 JSONL，事件索引保持固定大小并兼容旧内嵌索引在线迁移，消除 O(n²) 写放大。幂等、索引/投影失败恢复、损坏日志回退和 active Run 状态隔离回归通过；Work 全包沿用既有超 5 分钟无终态基线。 |
| Work 执行质量门禁 | `done` | `developping/work-quality-gate+2026-07-29` | `Codex + WorkGround2` | `internal/boot`, `internal/control`, `internal/work`, 对应测试 | Work Task Session 已改为业务交付语义并保留项目约束；新 Definition 为节点声明可观察验收条件，执行时追加独立质量复核；下游直接注入当前 revision 的依赖成果内容或可用工作区引用，缺失/损坏成果进入 `failed_retryable`；`web_search` 等硬能力必须有成功工具回执，原生搜索还需来源 URL，声明成果必须完整落槽。Control 全包、五类 Blueprint 全链、Boot 恢复与规划专项、相关 vet、根 CLI build、diff check 通过；Work 宽回归仍命中既有 artifact slot transition 与缺失 active definition 的基线失败。 |
| Work Block 单次提交与修订链恢复 | `done` | `developping/work-block-revision-submit+2026-07-29` | `Codex`（WorkGround2 调度超时后本地恢复） | `internal/work/service.go`, `internal/work/collaboration_recovery.go`, `desktop/frontend/src/work/controller.ts`, 对应测试 | 多字段 Block 的输入提交现在返回调度完成后的最终权威 Work revision，后续字段按返回值串行推进；未提交的 revision conflict 会刷新权威状态并用同一幂等请求安全重试一次。结构改进规划期间若只有运行时事件推进 revision、活动 Definition 未变，候选落库有界重基且不重复调用模型；真实 Definition 变化继续显式冲突。后端竞态回归连续 10 轮、相关 Work 测试、Controller 11 场景、ExecutionList 252 项、Store 85 项、TypeScript、前端生产构建、相关 vet 与 Desktop 编译通过；Work 宽回归仍命中 Feature Map 已记录的 Blueprint/DTO/ResultShelf/Cornerstone 基线失败。 |
| Work 启动恢复隔离 | `done` | `developping/work-recovery-startup-isolation+2026-07-29` | `Codex` | `internal/boot/boot.go`, `internal/boot/work_recovery.go`, `internal/work/v2_coordinator.go`, 对应测试 | 旧 Work 的 V2 调度恢复已移出 controller 同步启动关键路径；同一 Work 目录进程内单飞，controller 关闭时取消，日志与 Notice 显式报告结果。进程重启触发的自动恢复最多 3 次，之后保留 `failed_retryable` 供用户手动重试。Boot/Work 定向回归、相关 vet、diff check、Wails 正式构建通过；真实进程启动超过 30 秒后 controller 持续就绪，旧生日会任务未产生第 12 次尝试。Boot 全包仍命中既有 Tool 契约文档及 legacy eager/lazy MCP tier 三项基线失败；Work 全包运行约 4 分钟无终态后停止。 |
| Work AI 执行区完整规划预显 | `done` | `main`（共享脏工作树，未提交） | `Codex` | `desktop/frontend/src/work/components/v2/ExecutionList.tsx`, `ExecutionList.test.tsx` | Definition 确认后立即展示完整任务 DAG；尚未执行的节点以与 Go 一致的稳定 Task ID 显示为 `pending`，对应 TaskRuntime 到达后原地更新状态，避免 Block 随执行逐个新增。ExecutionList 252/252、TypeScript、生产构建与 diff check 通过；截图驱动的局部 UI 调整，按规则未使用 WorkGround2 Worker。 |
| Work 任务语言在提交时再确认 | `done` | `main`（共享脏工作树，未提交） | `Codex`（WorkGround2 503 后本地恢复） | `internal/work/dto.go`, `service.go`, `store_file.go`, `locale_draft_test.go`, `desktop/frontend/src/components/work/WorkCard.tsx`, `desktop/frontend/src/work/types.ts`, `desktop/frontend/src/__tests__/work-card.test.tsx` | 修复 Work 创建阶段过早锁定英文的问题：提交任务说明时用当前桌面 locale 更新权威 Work 语言，事件回放保持单一可信源，语言切换进入幂等请求签名；后续结构规划、输入 Block、成果标题/文件名及执行内容沿同一语言约束。WorkGround2 调度因 workspace 30 秒未就绪返回 503，未创建 Session，随后本地完成。Locale 后端专项、规划/修复/执行/文件名链路、相关包 vet、前端中文提交断言、生产 TypeScript、Vite 与独立 Wails 生产构建通过；验证版为 `desktop/build/bin/WorkGround2-locale-fix.exe`。WorkCard 全套仍在既有 ResultShelf/ExecutionList 基线处中断，测试 TypeScript 仍命中 Iris/ProjectTree/RunEvent 等既有错误，Work 全包约 3 分钟无输出后停止。 |
| Work 结构规划实时输出与结构确认 | `done` | `main`（共享脏工作树，未提交） | `Codex` | `internal/work/definition_planning_attention.go`, `internal/work/collaboration_recovery.go`, `internal/boot/definition_planner.go`, `desktop/frontend/src/components/work/WorkCardBack.tsx`, `StructureClarificationCard.tsx`, 对应测试 | 已删除“输入简短就询问确认节点”的固定题与启发式。Planner 新增 `structuralQuestions`，默认必须为 `[]`；只有意图/Base 无法区分多个会实质改变节点边界、依赖、并串行或槽归属的合理拓扑，且没有安全可逆默认值、只能由用户决定时，才允许返回一个问题。内容、可搜索信息、通用确认偏好及带推荐默认的问题均禁止；服务端校验影响范围、稳定 ID、2–4 个中立选项并派生流程，前端不预选答案，旧固定题缓存通过 schemaVersion 失效。Boot 新增契约测试通过；Work 专项排除共享工作树未完成的 `locale_draft_test.go` 后通过；回答卡 8/8、TypeScript、CSS、生产构建和相关 vet 通过。WorkGround2 Worker 派发超时且未返回 SessionID，由 Codex 本地完成；共享脏工作树未切分支、未提交。 |
| Work 输入文件与中文工作区语义 | `done` | `main`（共享脏工作树，未提交） | `Codex` | `internal/boot/definition_planner.go`, `internal/boot/definition_planner_test.go`, 既有 Work locale 链路 | 规划器明确将用户源文件建成 file InputSpec 并由首个真实任务消费，成果槽只表示新生成且要交付的内容；翻译示例改为“源 PDF 输入 → 翻译 PDF 成果”。前端当前 locale 已进入 Work 幂等创建意图并持久化，规划、修复、执行、Patch 与成果文件名共享语言约束。源文件/成果边界、三语提示、locale 持久化和执行继承、成果命名、WorkPage 29 项专项均通过；相关包 vet、前端类型检查与生产构建通过。`internal/boot` 全包 123 秒超时无结果，`internal/control` 全包受既有 Windows `.meta` 文件锁竞争失败阻塞，本次相关专项通过。截图驱动的桌面行为修复，按规则未使用 WorkGround2 Worker。 |
| Work 创建页窗口拖动 | `done` | `main`（共享脏工作树，未提交） | `Codex` | `desktop/frontend/src/components/work/WorkStartSurface.tsx`, `desktop/frontend/src/styles.css`, `desktop/frontend/src/__tests__/work-start-surface.test.tsx` | 创建任务说明页补回独立 Wails 标题栏拖动区，并为 Windows 原生窗控预留安全区域；输入编辑区保持 no-drag，避免影响输入和按钮交互。专项测试 11/11、CSS 语法/层级、TypeScript 与前端生产构建通过。 |
| Work 成果修改直接应用 | `done` | `main`（共享脏工作树，未提交） | `Codex + WorkGround2` | `desktop/frontend/src/work/components/v2/ResultShelf.tsx`, `ExecutionList.tsx`, `desktop/frontend/src/components/work/WorkCardFront.tsx`, 对应测试与样式 | 成果新增、修改、删除不再打开面向工程调试的 Workflow Patch 预览抽屉；用户单次确认后由后台执行预览校验、应用和权威刷新，仅展示简短进度、成功或可重试失败状态。任务讨论入口继续保留详细预览。验证：ResultShelf 251 项、ExecutionList 242 项测试通过，前端生产构建与 Wails 桌面构建通过。 |
| Work 操作后快照自动收敛 | `done` | `main`（共享脏工作树，未提交） | `Codex + WorkGround2` | `desktop/frontend/src/work/controller.ts`, `desktop/frontend/src/work/store.test.ts` | 操作后的普通快照若与本地投影处于同一 revision 但内容不同，自动升级为权威 retry resync；成功恢复时不向用户暴露“different snapshot”技术错误和手动同步按钮。验证：Work store 85/85、前端生产构建、Wails 桌面构建通过。 |
| Work 任务失败时成果终态收敛 | `done` | `main`（共享脏工作树，未提交） | `Codex + WorkGround2` | `internal/work/artifact_slot.go`、`internal/work/v2_settlement.go`、`internal/work/v2_coordinator_test.go` | 失败成果结算统一使用 ArtifactSlot 的真实幂等请求键；任务产物落盘失败后成果转为 failed/retryable，重复结算不再触发快照冲突，已有 Work 重开时可自动恢复。 |
| Work 运行进度悬浮窗 | `done` | `main`（按用户要求不切分支） | `Codex` | `desktop/frontend/src/components/work`, `desktop/frontend/src/styles.css`, 对应测试 | 运行进度从工作流正文移入标题栏状态按钮悬浮层；悬停临时预览，点击锁定并仅允许通过右上角 X 关闭。进度组件 57/57、类型检查、CSS 约束、生产构建与 Diff 检查通过；WorkCard 本次 11 条交互断言通过，完整脚本仍有共享脏工作树既有 V2 基线失败。 |
| Work Task 会话内聚展示 | `done` | `main`（共享脏工作树，未提交） | `Codex` | `internal/work`, `internal/control/taskexec.go`, `internal/control/task_live.go`, `internal/boot/boot.go`, `desktop/app.go`, `desktop/tabs.go`, `desktop/frontend/src/components/work`, `desktop/frontend/src/work/components/v2`, 对应测试与样式 | `work:` Task Session 从通用列表隐藏并在 Work Block 内打开；运行行始终保留不可选中的低对比走马灯，首 token 前显示“等待模型输出…”，之后实时展示 500ms 节流、360 字符尾部上限的模型输出。隐藏 Session 路径分配后即通过幂等入口持久化来源并写入 runtime，早于 Controller 构建与模型请求，悬停/聚焦 `(i)` 不再与首包同时缺席。Control 早期公布/幂等与 live 专项、Work 定向、相关 Go vet、Boot 编译门禁、ExecutionList 244/244、前端生产构建和独立 Wails 生产构建通过；Boot 宽匹配仍命中既有 legacy plugin tier 迁移基线失败。当前运行实例需重启以加载新嵌入资源。 |
| Work 结构化产物能力前置执行 | `done` | `developping/work-capability-preflight+2026-07-28` | `Codex + WorkGround2` | `internal/artifact`, `internal/work`, `internal/agent`, `internal/control`, 对应测试 | 结构化槽能力已从 Prompt 建议提升为 TaskExecutor 硬门禁：按 Definition 槽序与 ExpectedCount 串行执行 `request_help`，每个能力调用达到成功/失败终态后才允许主模型及 Shell fallback；成功制品沿同一 Session 历史进入共享 Producer/物化链路，失败结果显式留存并解锁 fallback，主模型收到终态摘要以避免重复求助。未来新增 CapabilityProducer 自动生成类型化 preflight。Control/Agent/Artifact 全包、Work preflight 与 NodePrompt 定向回归、相关包 vet、全仓 build 通过；Work 全包仍命中 Feature map 已记录的 definition revision/ready-set 基线失败。 |
| Work 通用 Session 产物能力继承 | `done` | `developping/work-session-artifact-parity+2026-07-28` | `Codex + WorkGround2` | `internal/artifact`, `internal/control/taskexec.go`, `internal/control/task_artifact.go`, `desktop/artifacts.go`, 对应测试 | 新增共享 Producer/Discovered/Collect 契约，Desktop Session 与 Work TaskExecutor 统一发现产物；图片生成结果及工作区文件经统一路径、symlink、大小、MIME/解码和 TOCTOU 校验后写入 Work BlobStore。Producer 可声明新 Kind，Work 按 Slot 顺序、发现顺序和 ExpectedCount 一次性确定分配，文本/Markdown/XLSX 保留兼容回退。Artifact、Control 全包、Desktop 产物专项及全仓 vet 通过；Desktop 全包 180 秒未结束，根 Fast 门禁仅命中 Feature map 已记录的 Boot/Serve/Work 既有基线失败。 |
| Work 产物能力路由与 Shell 兜底 | `done` | `developping/work-shell-artifact-discovery+2026-07-28` | `Codex + WorkGround2` | `internal/artifact`, `internal/work/scheduler_v2.go`, `internal/boot/definition_planner.go`, `internal/control`, `desktop`, 对应测试 | Artifact Producer 可通过共享 CapabilityProducer 契约声明 slot kind、能力名和执行指导；Work 运行时按 active slot 自动注入能力，image 槽优先调用 `request_help(image_generation)`，旧 Definition 无 hint 也生效，显式 hint 自动去重。Prompt 分离文本权威内容与结构化工具产物，并携带 slot 标题、类型、数量；新规划同步声明 image hint。Shell/脚本间接生成文件时，语言无关绝对路径扫描作为兼容兜底，图片按工作区边界、普通文件、大小、MIME 与解码重新校验。Artifact、Control 全包，Work/Boot/Desktop 定向回归及 root/desktop vet 通过。 |
| Work 详情标题栏收敛 | `done` | `developping/work-create-fun-status+2026-07-28`（共享脏工作树，未提交） | `Codex` | `desktop/frontend/src/components/work/WorkWorkspace.tsx`, `WorkCard.tsx`, `WorkCardFront.tsx`, `desktop/frontend/src/styles.css` | 删除正面重复的 Work 名称、运行摘要和“调整工作结构”入口；唯一运行入口移到固定标题区，保留翻面按钮作为结构调整入口；Windows 无边框窗口为系统控制区预留安全间距。CSS/层级检查、TypeScript 与 Vite 生产构建通过；WorkCard 本功能新增 DOM/CSS 契约全部通过，全套继续执行时受共享工作树既有 ResultShelf 无障碍断言及输入提交按钮缺失中断。用户明确要求不使用 WorkGround2 Worker。 |
| Work 输入提交后任务状态即时同步 | `done` | `developping/work-create-fun-status+2026-07-28`（共享脏工作树，未提交） | `Codex` | `internal/work/service.go`, `internal/work/v2_coordinator.go`, `internal/work/v2_coordinator_test.go` | 输入提交同步调度期间，每次权威 Task runtime 状态落库后立即发布 V2 WorkView，使 Block 从等待输入及时进入就绪/运行；发布失败独立累计并在调度收尾显式返回，不回滚或中断已持久化的业务状态。阻塞执行器回归确认 Submit RPC 未返回时已收到 running 快照，观察器失败回归确认已提交 runtime 不回滚；V2Coordinator、WorkStoreV2Authority、Boot、Control 定向测试、相关包 vet 与 diff check 通过。用户明确要求不使用 WorkGround2 Worker。 |
| Work 新建规划趣味状态文案 | `done` | `developping/work-create-fun-status+2026-07-28` | `Codex` | `desktop/frontend/src/components/work/WorkCardBack.tsx`, `desktop/frontend/src/__tests__/work-card.test.tsx` | 保存、规划、启动分别使用短梗文案池；进入状态随机展示，长耗时每 1.8 秒无连续重复轮播，屏幕阅读器使用稳定的正式状态播报。TypeScript、CSS 与 Vite 生产构建通过；临时跳过共享工作树两条既有中断用例后 WorkCard 329/332，本功能新增断言全部通过，余下 3 项为既有 ResultShelf 无障碍基线。用户明确要求不使用 WorkGround2 Worker。 |
| Work 执行任务状态流光边框 | `done` | `developping/work-create-auto-start+2026-07-28`（共享脏工作树，未提交） | `Codex` | `desktop/frontend/src/work/components/v2/ExecutionList.test.tsx`, `desktop/frontend/src/styles.css` | 执行任务 Block 直接按权威 TaskStateV2 呈现状态流光：运行态蓝青紫，需要输入/批准为琥珀橙，失败/失效为红粉橙；完成与普通等待态保持静态。现有图标和文字继续提供非颜色提示，动画遵循 reduced-motion。浏览器真实 CSS 渲染确认流光只占圆角边框且不覆盖内容；ExecutionList 203 项、CSS 检查、TypeScript 与 Vite 生产构建通过。用户明确要求不使用 WorkGround2 Worker。 |
| Work 成果卡操作与系统打开 | `done` | `developping/artifact-card-actions+2026-07-28`（共享脏工作树，未提交） | `Codex` | `desktop/frontend/src/work/components/v2/ResultCard.tsx`, `ResultShelf.tsx`, `desktop/frontend/src/App.tsx`, `desktop/work_artifact_file.go`, `desktop/frontend/src/styles.css` | 状态角标与修改/删除操作改为独立布局；打开与文件位置使用完整 Definition/Slot/Ref 身份校验，工作区文件直接访问，Blob 成果幂等物化为保留扩展名的只读本地文件后交给系统默认应用或文件管理器，旧 revision 与任意绝对路径均拒绝。ResultShelf 183 项、TypeScript、CSS、Desktop 定向测试、diff check 及独立 Wails 成品构建通过；真实视觉 QA 因正在运行的主实例独占 Work controller 而阻塞，详见 `design-qa-artifact-card-actions.md`。截图驱动的桌面 UI 改动，未使用 WorkGround2 Worker。 |
| Work 成果定义修改与格式迁移 | `completed` | `developping/work-artifact-edit+2026-07-28`（共享脏工作树，未提交） | `Codex` | `desktop/frontend/src/work/components/v2/ResultShelf.tsx`, `internal/boot/patch_planner.go`, `internal/control/task_artifact.go`, `internal/control/taskexec.go`, `internal/work/patch_service.go` | 成果卡增加“修改”入口，预填名称、格式、数量与是否必需；修改进入现有 Workflow Patch 预览/确认链路，保留 Slot ID 与生产/消费引用，原子更新 ArtifactSlotDef，并使生产任务及下游在新 Definition 修订下重跑。格式选择会同步已知扩展名；任务最终响应可将 Markdown 表格物化为真实 OOXML XLSX，避免只改名称或 MIME。ResultShelf 174 项、Patch/Planner/TaskArtifact 定向测试、TypeScript、CSS、Go vet 与 diff check 通过；完整包回归另发现并修正一项 runtime 测试前置状态。截图驱动的桌面交互改动，按规则未使用 WorkGround2 Worker。 |
| Work 必答问题与搜索能力 | `done` | `developping/work-required-input-search+2026-07-28` | `Codex + WorkGround2` | `internal/boot/definition_planner.go`, `internal/work/scheduler_v2.go`, 对应测试 | Definition Planner 只把完成工作不可缺且只能由用户提供的信息建成 required InputSpec，使用自然问题式 label/用途说明、按类型选择控件并绑定相关节点；孤立 required InputSpec 在解析期拒绝并进入既有有界修复，绑定后的输入沿 waiting_input 链路物化为同阶段友好 Block。公开信息改建 `web_search` 节点提示，执行时原生/现有搜索优先，缺失时复用 `request_help(web_search)` 的能力匹配、候选回退、来源校验与可重试错误；无路径时显式失败。Definition/Work/Agent 定向回归、相关包 vet 与 diff check 通过。 |
| Work 未修改 Block 数据跨修订保留 | `done` | `main`（共享脏工作树，未提交） | `Codex` | `internal/work/runtime_reuse.go`, `internal/work/definition_service.go`, `internal/work/patch_service.go`, `internal/work/runtime_reuse_test.go` | Workflow 修订创建新 Run 时，未受影响节点的已提交输入、BlockID、source/cornerstone 关联与完成 runtime 在同一事件批次迁移；新代际使用新 run/task/input 身份和新 digest，修改节点及下游仍失效，并只生成空的 requested input。重复 Apply 不重复输入。Definition/Workflow Patch 两条 FileStore 回归、runtime/input/patch 扩展组、Go vet 与 diff check 通过；`internal/work -short` 仍受共享工作树既有 Blueprint 数量、DTO fixture、ArtifactSlot 状态与 secret detector 基线失败阻塞。WorkGround2 Worker 创建会话时返回 workspace ready 503，未重复派发，由 Codex 本地完成。 |
| Work 提交后快照自动收敛 | `done` | `main`（共享脏工作树，未提交） | `Codex` | `desktop/frontend/src/work/controller.ts`, `desktop/frontend/src/components/work/WorkCard.tsx`, `desktop/frontend/src/work/store.test.ts` | 提交成功后的权威刷新改走可等待且并发合并的 Watch → authoritative retry snapshot 对账；同 revision 内容变化由后端权威快照自动收敛，不再短暂显示“刷新最新状态失败”或“快照错误/重试同步”，讨论窗在收敛后自动关闭。真实自动恢复失败仍显式暴露。Store 82/82、Controller 6 个场景、ExecutionList 198/198、TypeScript、CSS、生产构建及 diff check 通过。截图指向的局部同步/UI 修复，未使用 WorkGround2 Worker。 |
| Work 新增成果自动推断生产任务 | `done` | `main`（共享脏工作树，未提交） | `Codex` | `desktop/frontend/src/work/components/v2/ResultShelf.tsx`, `ResultShelf.test.tsx`, `desktop/frontend/src/styles.css`, `internal/boot/patch_planner.go`, `patch_planner_test.go` | 新增成果不再要求用户选择产出任务；Patch Planner 根据任务职责、依赖顺序和已有成果关系推断唯一生产任务，前端仅提供稳定预览锚点，预览中仍会明确展示实际受影响任务。ResultShelf 166 项、Patch Planner 定向测试、CSS、TypeScript 与 diff check 通过。截图指向的局部 GUI 调整，未使用 WorkGround2 Worker。 |
| Work Block 单一操作区 | `done` | `main`（共享脏工作树，未提交） | `Codex` | `desktop/frontend/src/work/components/v2/ExpandedBlock.tsx`, `ExecutionList.tsx`, `input/WorkInputHost.tsx`, `ExecutionList.test.tsx`, `desktop/frontend/src/styles.css` | 展开 Block 内所有字段共享唯一提交入口；提交前整组校验，提交过程按权威 revision 串行推进并保留逐项幂等恢复，全部结束后只刷新一次权威状态；移除底部收起与重复讨论，仅保留任务行顶部入口。ExecutionList 198 项、WorkInputHost 190 项、CSS、TypeScript 与生产构建通过。用户明确要求不使用 WorkGround2 Worker。 |
| Work 新建规划输入与名称编辑 | `done` | `developping/work-create-auto-start+2026-07-28` | `Codex` | `internal/work/service.go`, `internal/work/work_name_test.go`, `internal/work/collaboration_recovery.go`, `internal/work/definition_service.go`, `internal/work/event_v2.go`, `internal/work/store_file.go`, `desktop/frontend/src/components/work/WorkCard.tsx`, `desktop/frontend/src/components/work/WorkCardBack.tsx`, `desktop/frontend/src/__tests__/work-card.test.tsx` | 名称编辑已移除独立保存按钮，生成工作结构时与任务说明一次幂等保存；人工名称优先，初次规划未输入名称时由 LLM 规划目标推定并随候选提交原子落入 Work 投影。候选生成后自动应用，不再要求二次确认；恢复到已生成候选时也自动续接应用，成功后沿既有 revision 去重副作用翻到工作流正面。保存、规划或应用失败均显式提示并安全重试，应用失败只重试应用，不重复保存或调用 LLM。后端定向测试、Go vet、TypeScript、CSS 和 Vite 生产构建通过；跳过共享工作树两条既有中断用例后 WorkCard 324/327，余下 3 项为既有 ResultShelf 无障碍基线。用户明确要求不使用 WorkGround2 Worker。 |
| Work 已完成任务修改意见向下游传播 | `done` | `main`（共享脏工作树，未提交） | `Codex` | `internal/work`, `internal/boot/patch_planner.go`, `desktop/frontend/src/work` | 已完成任务的“修改意见”默认提升为 workflow 修订并安全重跑下游。receipt 恢复快照的成果槽已按后端一致的 `definitionRev + id` 组合身份合并，并允许新 authoritative V2 字段在 V1 快照重复时完成收敛；恢复入口和成果展示只消费当前 Definition 代际，真实同一组合身份/同一 revision 的内容冲突仍显式拒绝。本机 projection 确认 `joke_drafts` 在 Definition 2/3 均为 revision 2 的合法独立槽。Store 81、Controller 6、WorkInputHost 173、ResultShelf 154、恢复入口及生产构建通过。用户明确要求不使用 WorkGround2 Worker。 |
| Work V2 任务输入注入 | `done` | `developping/work-input-prompt+2026-07-27` | `Codex + WorkGround2` | `internal/work/scheduler_v2.go`, `internal/work/ready_set_test.go` | 当前 Work/Run/Task 范围内、由节点声明的 submitted/accepted WorkInput 已按 InputSpecIDs 与 Input ID 稳定顺序注入任务 Prompt；执行与产物阶段复用同一 Prompt，其他任务、Run、未声明 spec 和草稿值不会串入。9 项新增回归、相关既有 Scheduler 回归、race、vet 与 diff check 通过；完整 internal/work 仍受两个本功能外既有失败阻塞。 |
| Work 文本输入多行契约 | `done` | `main`（共享脏工作树，未提交） | `Codex` | `internal/work/input_value_schema.go`, `internal/boot/definition_planner.go`, `desktop/frontend/src/work/components/v2/input` | text valueSchema 已增加显式 multiline 维度，Definition planner 会为多行、列表及每行一项输入生成该标记；旧 Work 根据“每行/逐行/换行/多行/列表”提示兼容渲染四行 textarea，显式 false 可覆盖兼容推断。普通 Enter 换行，Ctrl/Cmd+Enter 沿用提交。WorkInputHost 178/178、后端 schema、planner prompt、TypeScript、CSS、Go vet 与 diff check 通过。用户明确要求不使用 WorkGround2 Worker。 |
| Work 成果增删与流程联动 | `completed` | `main`（共享脏工作树，未提交） | `Codex` | `internal/work/patch_path.go`, `internal/work/patch_service.go`, `internal/work/patch_artifact_test.go`, `internal/boot/patch_planner.go`, `desktop/frontend/src/work/components/v2`, `desktop/frontend/src/components/work/WorkCardFront.tsx` | 成果架可添加、删除成果；添加时由 Patch Planner 自动推断唯一产出任务，删除会列出生产/消费方。操作进入 workflow Patch 预览与确认链路，原子增删 ArtifactSlotDef、清理引用、生成新 Definition revision，并失效/重跑受影响任务；预览与正文均使用人类可读说明。已通过 Patch 定向测试、ResultShelf 166 项、ExecutionList 190 项、CSS 检查及前端生产构建。用户明确要求不使用 WorkGround2 Worker。 |
| Work 选择项 Schema 兼容恢复 | `done` | `main`（未提交） | `Codex` | `internal/work/input_value_schema.go`, `internal/boot/definition_planner.go`, `desktop/frontend/src/work/components/v2/input/schema.ts`, `WorkInputHost.tsx` | 新规划会把 choice / multi_choice 的 options 规范为 `{value,label}` 数组；旧 Definition 的对象、分隔字符串和字符串数组可继续显示并提交；完全缺少候选项时，单选降级为文本框，多选降级为“每行一项”输入并在提交时转成去重字符串数组；不可恢复错误改用人类可读提示。后端定向测试、Definition Planner 测试、WorkInputHost 190 项及 TypeScript 检查通过。小改动，未使用 WorkGround2 Worker。 |
| Go 测试执行提速 | `done` | `main`（未提交） | `Codex` | `Makefile`, `scripts/test.ps1`, `internal/boot/*_test.go`, `internal/work/*_test.go`, `internal/acp/live_test.go`, `internal/provider/openai/realcache_test.go` | 默认测试恢复 Go 缓存，新增 Fast/Root/Fresh/Desktop/All/Live 分层；真实模型测试需 `live` tag 与 `WORKGROUND2_LIVE_TEST=1` 双门禁。`internal/work -short -count=1` 从超过 60 秒降至 9.6 秒，整仓 Fast 在 48.8 秒内完成并显式报告当前分支既有失败；文件存储、跨实例、完整 Blueprint 链路仍由非 short 入口覆盖。 |
| Work LLM 交互诊断日志 | `done` | `developping/work-llm-interaction-logging+2026-07-27` | `Codex + WorkGround2` | `internal/config`, `internal/boot/definition_planner.go`, `internal/boot/patch_planner.go`, `internal/boot/work_llm_logger.go` | `[work].llm_interaction_log` 控制 Definition/Patch planner 每次请求、原始响应、stream/chunk/parse error 的并发安全 JSONL 诊断记录；默认关闭，本机已临时开启。Config/Boot 定向测试、并发写 race、vet 与 diff check 通过。 |
| Work Definition planner JSON 恢复 | `done` | `main`（共享脏工作树，未提交） | `Codex` | `internal/boot/definition_planner.go`, `internal/boot/definition_planner_test.go`, `internal/boot/work_llm_logger_test.go` | 双层收敛模型输出：首轮与修复提示词禁止解释和复述 JSON，修复轮仅携带最后一个完整对象候选；解析器从后向前选择最后一个通过 DefinitionPlan 严格 DTO 契约的对象，分析文字、数组、示例和非法对象不阻塞恢复。无合法对象时保留显式错误和原有三次安全重试。Definition/Repair 定向回归与全仓 vet 通过；完整 boot 回归受共享工作树既有 `request_help` 文档和 plugin tier 迁移 3 项失败阻塞，全仓测试在 244 秒门限内未结束。用户明确要求不使用 WorkGround2 Worker。 |
| AI 协作确定性 Skill Bundle | done | `developping/ai-collaboration-skip-meta-work+2026-07-21` | Codex | `desktop/ai_collaboration_app.go`, `desktop/ai_collaboration_app_test.go`, `desktop/ai_collaboration_skill` | Bundle 1.0.1 将安装/创建/更新 Skill 与生成设计文件列为明确直执任务，导出提示词、运行规则和内嵌 Skill 均禁止把这两类操作调度给 WorkGround2。专项 Go、vet、PS1 解析和 Skill 结构校验通过。 |
| Work Block 版本与四槽布局 | `done` | `developping/work-block-schema-placement+2026-07-27` | `Codex` | `internal/work`, `desktop/frontend/src/components/work`, `desktop/frontend/src/styles.css`, `docs/WORK_SYSTEM_DESIGN.zh-CN.md` | kind 独立的 BlockSchemaRegistry 已统一写入、刷新、Blueprint 校验与重执行迁移；file_list v1→v2 迁移保留源归档并在 latest rerun 使用目标 Placement。Desktop 按 attention、primary、secondary、result 四槽及 12 栏 span 响应式渲染，保留 V2 固定 ResultShelf/ExecutionList。Work 前端全套、生产构建、后端专项与全仓 vet 通过；全量 Go 门禁受基线已存在的 TestReduceUpdate_InvalidTransition 失败阻塞。 |
| Work 系统 V1 | `done` | `developping/work-v1-product-loop+2026-07-23` | `Codex` | `internal/work`, `internal/control/work.go`, `desktop/works.go`, `desktop/frontend/src/work`, `desktop/frontend/src/components/work`, `docs/WORK_SYSTEM_DESIGN.zh-CN.md` | 产品闭环已补齐：空 Prompt 在 UI/Service 运行前显式阻断；Draft/可编辑 Block 写入事件日志；创建页读取 Blueprint；进行中/历史/回收站支持归档、恢复、删除；复制与原定义重执行创建独立 Draft，最新定义缺少迁移器时显式阻断。历史 writer 仅作审计，重启写入由 OS lease 保护。 |
| Work 协作工作台 V2 | `in_progress` | `developping/work-collaboration-workbench+2026-07-23` | `Codex` | `docs/WORK_COLLABORATION_WORKBENCH_V2.zh-CN.md`, `internal/work/v2.go`, `internal/work/event_v2.go`, `desktop/frontend/src/work/types_v2.ts`, `desktop/frontend/src/__tests__/work-v2-contract.test.ts` | 成果驱动的人机协作工作台。Ctrl_CC Feature: `feat_1784804783784_78d81337b5`。状态: 契约返修/待验收；等待输入时幂等物化权威 WorkInput，同一阶段输入合并为紧凑 Block，Tab 按字段顺序前进并在末项离开；提交桥接使用公共 `SubmitWorkInputRequest` 并显式映射 `definitionRevision`，避免版本被解码为 0 导致未确认，后端拒绝原因会原样暴露；Submit 与 pin/unpin 的 `expectedRevision` 使用权威 Work revision，`inputRevision` 独立保留 Input revision，避免把 Input 版本 0 错当成 Work 版本引发冲突；Work 列表页补齐 header、筛选、条目与操作区样式契约，避免原生按钮退化；Work 数据仍按 Workspace 隔离，排障时先核对桌面当前 Workspace，禁止把切库误判为数据丢失；补齐任务最终响应成果落库、严格输出契约、历史缺失成果幂等修复、reserved 成果直接重新生成、Run/Work 聚合结算和前端恢复入口；讨论 Block 支持旧 Work 原子物化，Patch planner 非 JSON 有界修复；跨基础 Work/V2 mutation 使用完整快照，修复同 revision 半投影冲突。 |
| Work 讨论模态层 | `done` | `developping/work-special-session+2026-07-27` | `Codex + WorkGround2` | `desktop/frontend/src/work/components/v2/discussion/DiscussionDrawer.tsx`, `desktop/frontend/src/work/components/v2/discussion/discussion.css`, `desktop/frontend/src/work/components/v2/discussion/discussion.test.tsx`, `desktop/frontend/src/work/components/v2/ExecutionList.tsx`, `desktop/frontend/src/work/components/v2/ExecutionList.test.tsx`, `desktop/frontend/src/components/work/WorkCardBack.tsx` | 将讨论输入从 ExecutionList 文档流提升为应用级模态层；底层工作流不再被推挤。V2 Work 全生命周期不挂载默认 SessionSurface，消除背面默认对话及 3D 镜像穿透；仅明确选择关联任务 Session 时显示对应会话。Apply 使用稳定讨论目标与操作 epoch 接收响应，结构事件先于 RPC 回执更新 Definition revision、切换活动 Run 时仍会完成权威刷新并自动关闭；失败与不完整回执继续保留窗口供重试。 |
| Work 特殊 Session | `done` | `developping/work-special-session+2026-07-27` | `Codex + WorkGround2` | `internal/agent/branch.go`, `internal/agent/save.go`, `internal/work/service.go`, `desktop/tabs.go`, `desktop/works.go`, `desktop/frontend/src/App.tsx`, `desktop/frontend/src/components/ProjectTree.tsx`, `desktop/frontend/src/components/work/WorkCardBack.tsx` | Work 已收敛为 Workspace 下的特殊 Session；规划首屏隐藏 0 节点结构和默认 Session Composer，只保留任务说明主输入；保存任务说明时由模型层派生 Work.Name，并以同一权威视图幂等同步 Session/Topic/Tab/Meta 标题，Work Session 不提供手动重命名。标题同步失败显式返回已提交 revision，同 requestId 可安全重试。Work/App 专项、Go vet、TypeScript 与生产构建通过；WorkCard 309/311，余下 2 项为既有 artifact 主机边界失败。 |
| Work 自然语言输入 | `done` | `developping/work-ui-simplification+2026-07-23` | `Codex` | `internal/work/dto.go`, `internal/work/service.go`, `desktop/frontend/src/components/work/WorkPage.tsx`, `desktop/frontend/src/components/work/WorkCardBack.tsx`, `desktop/frontend/src/styles.css` | 创建和编辑只向用户暴露自然语言任务说明；名称从 Prompt 自动生成，Blueprint 与 Inputs 保留为内部模型。运行页本轮保持不变。Work 前端全套测试、生产构建和 Service Create/UpdateDraft 专项通过。 |
| 小组件缩小态布局与皮肤适配 | done | `developping/widget-compact-skins+2026-07-19` | Codex | `desktop/frontend/src/components/widget`, `desktop/frontend/src/__tests__/widget-layout-contract.test.ts`, `desktop/frontend/src/lib/bridge.ts`, `docs/desktop/widget-mode-design.md` | WebView2 桌面缩放已由逻辑画布与逆 transform 抵消；电子宠物皮肤进一步把信息层右沿收回左侧小屏，并让 230px 角色精灵以小屏宽度为硬上限。125% 缩放的 416×128 CSS 视口下，角色保留 3.6px 屏内右边距、距正文 43.6px，外壳与右边框完整；Widget 布局/信息/皮肤资源专项、TypeScript、前端生产构建及 Wails 生产打包通过。 |
| 小组件窗口几何恢复 | done | `developping/widget-window-geometry+2026-07-17` | Codex | `desktop/widget_mode.go`, `desktop/window_state.go`, `desktop/widget_window_windows.go`, `desktop/widget_window_windows_test.go`, `desktop/app.go` | Windows 启动恢复和模式切换绕过 Wails 不对称的位置写入，使用绝对屏幕坐标与 DPI 尺寸并读回验证；后端在模式锁内重采样窗口状态，迟到前端保存无法跨模式污染。bounds/Region 失败回滚且不发布模式。Widget 专项、并发 race、Desktop vet 通过。 |
| 小组件窗口切换一致性 | done | `developping/widget-window-transition+2026-07-17` | Codex（WorkGround2 越界后本地恢复） | `desktop/widget_mode.go`, `desktop/widget_mode_test.go` | Enter/Exit 的模式判定、独立几何持久化、原生窗口尺寸/位置/Region/置顶/最大化恢复与状态发布已收敛到同一互斥入口；同向调用幂等，反向调用串行，失败保持旧模式并可重试。Widget 专项、并发 race、Desktop vet 通过；Desktop 全量仍受既有 Darwin CLI 路径、Topic Tree/runtime detach/session lease 用例失败阻塞。 |
| 小组件前端状态同步与快捷切换 | done | `developping/widget-skins+2026-07-17` | Codex（WorkGround2 状态 API 异常后本地收敛） | `desktop/frontend/src/App.tsx`, `desktop/frontend/src/lib/widgetModeCoordinator.ts`, `desktop/frontend/src/lib/keyboardShortcuts.ts` | 原生 `widget:mode`、标题栏/Composer 入口、Ctrl+M 与 React 状态已收敛到可重试协调器；成功回包和原生事件均可立即显示 Widget，并用显式 class 隐藏主界面。协调器/快捷键/设置契约/布局专项、TypeScript 与前端生产构建通过。 |
| 小组件原生重绘与跨屏可见性 | done | `developping/widget-skins+2026-07-17` | Codex（WorkGround2 status 404 后本地接管） | `desktop/widget_window_windows.go`, `desktop/widget_window_windows_test.go`, `desktop/widget_mode.go` | Region 变化后按 `FRAMECHANGED → RedrawWindow(含子 WebView) → DwmFlush` 强制刷新合成表面；每次进入前枚举显示器工作区，保留仍可见的原屏位置，旧屏移除时回退当前主窗口所在屏，并按目标 DPI 将完整窗口夹回工作区。普通专项、race、vet 与实际 Desktop EXE 构建通过。 |
| Session 背景图片 | done | `developping/session-background+2026-07-17` | Codex（WorkGround2 Desktop 503 后本地恢复） | `internal/config`, `desktop/session_background_app.go`, `desktop/frontend/src/components/SessionBackground.tsx`, `desktop/frontend/src/components/SettingsPanel.tsx`, `desktop/frontend/src/App.tsx`, `desktop/frontend/src/styles.css` | 用户级 Desktop UI-only 配置；支持多个图片和文件夹、来源启停/递归、浅色/深色自适应渐变遮罩、Session 打开随机选图、可配置定期轮换、后台暂停与坏图自动跳过。配置写入、来源扫描、Tab 选图均幂等且缺失来源可恢复；专项 Go/前端测试、Desktop/root vet、TypeScript、CSS/z-index、前端生产构建及 Wails 生产打包通过。全仓与 Desktop 全量仍受既有 Boot 工具契约、Serve 语言偏好、Darwin 路径、Topic Tree/session lease 等测试失败阻塞。 |
| Session 背景布局修复 | done | `developping/widget-skins+2026-07-17` | Codex | `desktop/frontend/src/App.tsx`, `desktop/frontend/src/styles.css`, `desktop/frontend/src/__tests__/workbench-layout.test.ts`, `desktop/frontend/src/__tests__/session-background.test.tsx` | Workbench 网格列与固定 264px 侧栏统一，避免其他布局持久化宽度产生 36px 空轨；背景状态栏改为主题半透明磨砂层。真实页面计算间隙为 0px，前端专项、类型检查、CSS 检查、生产构建及 Wails 打包通过。 |
| Workbench 运行记录随轮次滚动 | done | `developping/widget-skins+2026-07-17` | Codex | `desktop/frontend/src/App.tsx`, `desktop/frontend/src/components/Transcript.tsx`, `desktop/frontend/src/components/desktop-ui/IrisInfoComponents.tsx`, `desktop/frontend/src/lib/runEvents.ts`, `desktop/frontend/src/styles.css` | 实时与历史运行统一绑定 `turn:n`，记录插入对应对话轮次并随历史上移；Transcript 尾部只保留无轮次事件兜底。结束记录行移除外层填充与底色，保留独立圆角状态条。运行投影 40 项、布局筛选 5 项、CSS、全量类型检查及前端生产构建通过。 |
| 外部会话过期清理索引收敛 | done | `developping/external-session-expiry-index+2026-07-17` | Codex + WorkGround2 | `desktop/external_session_gc.go`, `desktop/external_session_gc_test.go`, `internal/session/session.go`, `internal/session/artifacts_test.go` | CLI/IM 外部会话进入回收站后会在 Topic 索引事务锁内二次确认任意 live Session/runtime；仅孤儿 Topic 会从项目、标题、来源和创建时间索引移除，历史回收站遗留项在首次 sweep 幂等对账，失败显式记录并可在下次 sweep 重试。task-memory sidecar 已纳入 trash/restore/purge 生命周期。GC/DeleteSession 专项、race、task-memory 回收恢复及 Desktop/session vet 通过；按用户要求未重启或替换当前正在工作的 WorkGround2，修复将在其下次启动时生效。 |
| Workbench SessionList 会话切换恢复 | done | `developping/session-list-switch+2026-07-17` | Codex（WorkGround2 Desktop CLI 不可达后本地恢复） | `desktop/tabs.go`, `desktop/tabs_order_test.go` | 根因是 `keepOnlyVisibleTab` 持有 `App.mu` 执行 Snapshot，CLI session 发生磁盘冲突时恢复回调再次获取同一锁而自锁，首个导航请求与后续点击全部挂起。现已把 Snapshot/元数据 I/O 移出全局锁，并在裁剪前重新校验 tab 集合；新增可重入 Snapshot 防死锁回归。专项、race、desktop vet、Wails 生产构建通过；修复版桌面已连续切换普通/CLI session 完成可视验收。desktop 全量仍有 Darwin 路径、Topic Tree、session runtime/lease 等既有失败，已在未含本修复的 `0f7ff2d` 基线复现。 |
| Desktop 会话身份与路由收敛 | done | `developping/session-id-routing+2026-07-16` | Codex（WorkGround2 Desktop 不可达后本地恢复） | `desktop/session_registry.go`, `desktop/app.go`, `desktop/tabs.go`, `desktop/remote_api.go`, `desktop/frontend/src` | 已建立以稳定 `SessionID` 为键、互斥保护增删/换绑的 Session Registry；External 每次创建产生新 ID，查询与操作强制显式 ID；UI 分离缓存 TabID/SessionID，切换与单会话布局不再清理仍可查询的后台 Runtime。小组件会在 session lease 释放后自动重试启动，真实占用或重试失败时保留显式错误并允许继续恢复。Registry/Remote/Desktop 专项及 race、Desktop vet、前端定向测试、TypeScript 和生产构建通过；Desktop 全包既有 Darwin/Topic Tree/session lease 等失败已在改动前基线复现。 |
| 本地图片会话内自动预览 | done | `developping/local-image-inline+2026-07-16` | Codex + WorkGround2 | `desktop/artifact_image.go`, `desktop/frontend/src/components/ArtifactImageCard.tsx`, `desktop/frontend/src/components/Transcript.tsx`, `desktop/frontend/src/lib/bridge.ts`, `desktop/frontend/src/__tests__/artifact-image-display.test.tsx` | 任意已完成工具只要产生当前会话内已验证的可用图片产物，就按 `sourceRunId` 在折叠区外自动展示；支持普通 shell、导出/渲染工具、嵌套工具、`request_help` 和单工具多图。图片读取按 tab 与 artifact ID 重新对账并校验目录、普通文件、大小、MIME、解码和读取稳定性，不信任任意文本路径。Go 图片/产物专项、Desktop vet、28 项通用图片测试、35 项 request_help 回归、36 项会话分组、TypeScript、CSS/z-index 检查及生产构建通过。 |
| 能力求助图片制品折叠外展示 | done | `developping/request-help-artifact-visible+2026-07-15` | Codex + WorkGround2 | `desktop/frontend/src/components/Transcript.tsx`, `desktop/frontend/src/__tests__/request-help-display.test.tsx` | 已完成且带结构化图片制品的 `request_help(image_generation)` 卡片直接展示在“思考过程”折叠外；其余推理、普通工具、运行中/失败/无制品求助仍保持原折叠语义，空折叠不渲染且图片不重复。图片专项 53 项、会话分组 36 项、TypeScript、前端生产构建和 Wails 生产构建通过；修复版桌面进程已启动并对历史会话完成可视验收。 |
| 外观新增配色方案 | done | `developping/appearance-color-palettes+2026-07-16` | Codex + WorkGround2 | `desktop/frontend/src/lib/theme.ts`, `desktop/frontend/src/styles.css`, `desktop/frontend/src/components/SettingsPanel.tsx`, `desktop/frontend/src/locales`, `desktop/frontend/src/__tests__/theme-new-styles.test.ts` | 沿用现有外观页与主题卡片结构，新增翡翠雾、钴海、勃艮第、数字雨、忍火、机甲樱六套浅色/深色/跟随系统配色；创建页强调色同步且无跨主题串色，浅色主要操作色按 WCAG 对比度收紧。216 项主题契约、8 项自动主题契约、TypeScript 与生产构建通过。 |
| 生成图片登记到产物架 | done | `developping/generated-image-artifact+2026-07-16` | Codex + WorkGround2 | `desktop/artifacts.go`, `desktop/artifacts_test.go` | `request_help(image_generation)` 返回的结构化图片已纳入当前会话产物投影；仅允许经桌面边界重新校验的受信输出目录图片，工作区外图片显示稳定文件名，重复路径幂等去重并支持历史恢复。产物专项测试与 Desktop `go vet` 通过；Desktop 全包受既有 Darwin CLI 路径、Topic/Project Tree 与 session lease 清理用例失败阻塞。 |
| 桌面传呼机小组件模式 | done | `developping/widget-usability-routing+2026-07-16` | Codex + WorkGround2 | `desktop/widget_mode.go`, `desktop/widget_conversation.go`, `desktop/widget_mode_test.go`, `desktop/frontend/src/components/widget`, `desktop/frontend/src/locales`, `desktop/frontend/src/assets/widget-mode`, `docs/desktop/widget-mode-design.md` | 延续单消息传呼机模型。默认窗口为 `590 × 176`；支持 workspace 自动/手动路由、幂等回执和完成态 session 激活。交互界面适配简中、繁中、英文，每种语言各有 40 条短运行文案，4–7 秒轮播且避免连续重复；点阵信息轮播统一保留英文。后端用稳定语义码传递状态、路由原因和审批选项，并兼容旧中文数据。Widget 专项、Desktop vet、前端类型/文案/CSS/生产构建通过；Desktop 全量测试仍受既有 Darwin 路径、Topic/Project Tree、session runtime/lease 用例失败阻塞。 |
| 小组件点阵信息轮播 | done | `developping/widget-info-carousel+2026-07-16` | Codex | `desktop/widget_info.go`, `desktop/widget_mode.go`, `desktop/frontend/src/components/widget/WidgetInfoCarousel.tsx`, `desktop/frontend/src/components/widget/widget-info-carousel.css`, `desktop/frontend/src/assets/widget-mode`, `docs/desktop/widget-mode-design.md` | 左侧身份区已调整为可点击的六页英文点阵终端：TOKEN 累计、时钟、W2 电子宠物、IDLE 时长、网络/CPU/内存、已接入模型；主轮播手动切换并恢复上次页面，任务上下文按新 revision 抢占一次且尊重用户后续切换。Widget 专项、Desktop vet、前端类型检查/生产构建与 590×176 浏览器视觉 QA 通过；Desktop 全量仍受既有 Darwin 路径、Topic/Project Tree 与 session lease 用例失败阻塞。 |
| 能力求助图片制品超时恢复 | done | `developping/request-help-artifact-recovery+2026-07-15` | Codex + WorkGround2 | `internal/agent/request_help.go`, `internal/agent/request_help_test.go`, `internal/agent/coordinator_test.go` | `request_help(image_generation)` 在辅助模型已上报有效 CLI 图片但最终文本超时/失败时，先按既有边界、普通文件、MIME 与解码规则校验，成功则恢复候选为 completed 并返回结构化制品及原始错误诊断；无效制品仍显式失败且不切换第二候选。图片专项、Agent 全包、Boot RequestHelp 专项、内置工具全包及全仓 vet 通过；全仓测试受既有 `internal/serve/TestServeIndexPagePassesLanguagePreferenceToClient` 失败阻塞，Boot 全包联合命令 10 分钟无输出超时。 |
| Boot 测试远程 Skill 隔离 | done | `developping/request-help-artifact-recovery+2026-07-15` | Codex | `internal/boot/boot_test.go` | Boot 测试统一把 WorkGround2 Home、State、Cache 定向到临时目录，Windows 不再读取 `%APPDATA%` 中的真实 Flow SkillShare profiles；专项 trace 确认无 `fetchRemoteSnapshot` 和远程 HTTP 调用。 |
| 对话图片制品展示 | done | `developping/conversation-image-artifact+2026-07-15` | Codex + WorkGround2 | `internal/agent/assist_artifact.go`, `desktop/app.go`, `desktop/request_help_image.go`, `desktop/frontend/src/lib/requestHelp.ts`, `desktop/frontend/src/components/RequestHelpCard.tsx`, `desktop/frontend/src/styles.css`, `desktop/frontend/src/__tests__/request-help-display.test.tsx` | `request_help(image_generation)` 的已校验图片已作为结构化制品贯穿实时结果、压缩历史和桌面对话；支持尺寸信息、缩略图、全屏预览、安全打开/定位、复制路径、文件失效提示和可重试加载。所有本地读取与操作均重新校验输出目录边界、普通文件、大小、MIME、解码和文件身份。Agent/Provider 与 Desktop 专项、全仓 Go vet、41 项前端专项、TypeScript 和生产构建通过；根模块全量测试 10 分钟无输出超时，Desktop 全量受既有 Topic/Project Tree 与 session lease 用例失败阻塞，单独运行 `TestCreateTopicDefaultsToAutoNewSessionTitle` 可复现。 |
| 能力求助触发补全 | done | `developping/assist-capability-detection+2026-07-15` | Codex + WorkGround2 | `internal/config/capability.go`, `internal/config/cli_capability.go`, `internal/provider/artifact.go`, `internal/provider/cli`, `internal/agent/request_help.go`, `internal/agent/assist_artifact.go`, `docs/GUIDE*.md`, `docs/SPEC.md` | Codex CLI 自动识别并接管图片生成，以 JSONL thread ID 收集请求作用域制品并校验路径边界、文件、MIME 和解码；Google/Gemini 按模型识别网页搜索、识图、画图能力，显式 capabilities 含空数组始终优先。Config/Provider/Agent/Boot/Desktop 专项测试和 vet 通过；真实 DeepSeek 对话已触发 `request_help(image_generation)` 并由 `local-codex/gpt-5.5` 生成有效 PNG。 |
| 能力求助状态展示 | done | `developping/request-help-display+2026-07-15` | Codex + WorkGround2 | `internal/agent/request_help.go`, `desktop/app.go`, `desktop/frontend/src/components/RequestHelpCard.tsx`, `desktop/frontend/src/components/ToolCard.tsx`, `desktop/frontend/src/lib/requestHelp.ts`, `desktop/frontend/src/lib/useController.ts`, `desktop/frontend/src/styles.css`, `desktop/frontend/src/locales`, `desktop/frontend/src/__tests__/request-help-display.test.tsx` | `request_help` 触发后在对话流中立即显示专属内联状态，持续呈现发起模型、接管模型、能力、尝试次数、候选切换及成功/失败；视觉以 Product Design 方案 1 为基准，支持历史回放、压缩归档、详情展开、窄屏布局和安全重试状态。Go/前端专项测试、vet、typecheck、生产构建和浏览器视觉验收通过。 |
| 能力求助路由 | done | `developping/capability-assist+2026-07-15` | Codex + WorkGround2 | `internal/config`, `internal/agent/request_help.go`, `internal/agent/assist_artifact.go`, `internal/boot/boot.go`, `desktop/onboarding_cli.go`, `desktop/settings_app.go`, `desktop/frontend/src/components/SettingsPanel.tsx` | 主模型缺少网页搜索或图片生成能力时，默认通过宿主 `request_help` 路由到已配置且明确声明能力的候选；支持稳定 request ID、安全失败切换、来源/图片制品校验、运行状态 transcript 和桌面能力开关。Codex CLI 启动时探测实际浏览能力，仅自动用于网页搜索。核心 Config/Agent 全量、Boot/内置工具专项、Desktop 专项、Go vet、全仓 Go build 与前端生产构建通过；Boot 全包曾运行 244 秒后超时，无明确失败。 |
| 会话运行态与回复条件即时适配 | done | `developping/session-runtime-reply+2026-07-15` | Codex + WorkGround2 | `desktop/frontend/src/lib/useController.ts`, `desktop/frontend/src/App.tsx`, `desktop/frontend/src/components/Composer.tsx`, `desktop/frontend/src/components/desktop-ui`, `desktop/frontend/src/__tests__` | 切回运行中的 session 时，首帧从全局 tab 快照同步恢复运行提示与回复路由；展示态按 `RuntimeMode` 区分运行中、等待用户、后台运行和取消中，回复条件统一以 `ForegroundActive` 决定“发送/加入队列”。保留迟到状态对账、stale idle 防回退及异步提交二次判定。相关 482 项专项回归、TypeScript 检查和前端生产构建通过；完整 Iris 集成脚本受既有 jsdom `scrollIntoView` 缺口中止。 |
| 智谱预设升级 GLM-5.2 | done | `developping/glm-5-2-preset+2026-07-15` | Codex + WorkGround2 | `desktop/settings_app.go`, `desktop/app_test.go`, `desktop/frontend/src/locales`, `desktop/frontend/src/__tests__/provider-choices-contract.test.ts` | 智谱清言默认模型已升级到官方 `glm-5.2`，上下文更新为 1,000,000；保留 GLM-5.1/4.7/4.6 等回退模型并同步三语文案。Go 智谱/幂等专项、28 项前端契约和生产 TypeScript 检查通过。 |
| 智谱、豆包与本地 Qwen 供应商预设 | done | `developping/ai-provider-presets+2026-07-15` | Codex + WorkGround2 | `desktop/settings_app.go`, `desktop/app_test.go`, `desktop/frontend/src/components/SettingsPanel.tsx`, `desktop/frontend/src/locales`, `desktop/frontend/src/__tests__/provider-choices-contract.test.ts` | 设置 > 模型 > 添加供应商已增加智谱清言、豆包和本地 Qwen。云服务使用 OpenAI-compatible API Key 模板；本地 Qwen 默认连接 Ollama、无需 Key，可刷新本机模型并允许修改兼容端点。重复添加幂等。供应商专项、25 项新增前端契约、18 项 provider model、29 项模型设置、TypeScript、生产构建和 go vet 通过；Desktop 全量测试受既有 Remote API、Topic Tree、session lease/临时目录清理用例失败阻塞，失败可在排除本功能测试时独立复现。 |
| 自适应新会话页 | done | `developping/adaptive-new-session+2026-07-14` | Codex（WorkGround2 调度失败后本地恢复） | `desktop/workspace_welcome.go`, `desktop/frontend/src/components/Welcome.tsx`, `desktop/frontend/src/lib/welcome.ts`, `desktop/frontend/src/lib/useWorkspaceWelcome.ts`, `desktop/frontend/src/App.tsx`, `desktop/frontend/src/styles.css`, `desktop/frontend/src/locales`, `desktop/frontend/src/__tests__/welcome-adaptive.test.tsx` | 新会话页按 Workspace 内容、会话历史和双轴熟悉度生成“一主两辅”；支持代码、文档、数据、媒体、调研、混合、空与 Global，建议仅填入并聚焦 Composer。画像扫描限深、限量、可超时重试、可缓存降级，跨 Workspace 响应有代次保护。字符画、表情、冷笑话和 460ms MicroGlitch 默认低频启用，前 3 次禁用、30 天去重、尊重减少动态效果并可关闭。专项 Go、生产 TypeScript、25 项欢迎页测试、CSS 检查与前端生产构建通过；Desktop 全量 Go 测试 184 秒超时，测试专用 TypeScript 配置仍有本功能外既有错误。设计基准：`docs/ADAPTIVE_NEW_SESSION_DESIGN.zh-CN.md`。 |
| 外部会话空闲自动回收 | done | `developping/external-session-idle-trash+2026-07-14` | Codex | `internal/agent`, `internal/bot`, `internal/botruntime`, `desktop` | IM 自动会话空闲 4 小时、CLI 远程会话空闲 6 小时后移入回收站；pin、运行中、会话 lease 与可恢复清理状态豁免。受影响 Go/前端专项测试与 go vet 通过。 |
| CLI 会话中新建桌面会话 | done | `developping/cli-session-new-session+2026-07-14` | Codex + WorkGround2 | `desktop/tabs.go`, `desktop/session_source_test.go` | workspace “+” 不再复用 CLI 来源 tab；运行时 source 与持久化 sidecar 均可识别，新建 desktop blank 后仍保持普通幂等复用。专项测试与 go vet 通过；desktop 全量测试受既有 topic-tree/cache、session lease 用例失败阻塞。 |
| 全局会话状态与待关注跳转 | done | `developping/global-session-status+2026-07-14` | Codex + WorkGround2 | `internal/control`, `desktop/app.go`, `desktop/tabs.go`, `desktop/remote_api.go`, `desktop/desktop_cli.go`, `desktop/frontend/src/App.tsx`, `desktop/frontend/src/components/SessionStatusIndicators.tsx`, `desktop/frontend/src/components/ProjectTree.tsx`, `desktop/frontend/src/__tests__` | Running 与待关注口径已统一；当前活动、正在查看的 session 完成时保持已读，后台或已切走的桌面 session 完成后进入待关注。CLI 会话支持桌面接管，外部 CLI 创建/提交走后台 tab，状态与交互按远程目标 session 路由。专项回归及 go vet 通过。 |
| 本地 CLI 一级接入与自动扫描 | done | `developping/local-cli-provider-entry+2026-07-12` | Codex + WorkGround2 | `desktop/onboarding_cli.go`, `desktop/frontend/src/components/SettingsPanel.tsx`, `desktop/frontend/src/locales`, `desktop/frontend/src/__tests__` | 本地 CLI 已成为添加供应商第三个一级分支；进入即自动扫描，只展示已安装项，可重新扫描并一键添加使用。已有可用 API provider 时 CLI 只加入可选列表，不覆盖 API 默认模型；没有可用 API 时仍以 CLI 兜底。真实桌面包已识别本机 Codex CLI 0.130.0-alpha.5；前端专项、类型检查、构建和 desktop Go 测试通过。 |
| 桌面设置持久化防覆盖 | done | `developping/settings-persistence-race+2026-07-12` | Codex | `desktop/app.go`, `desktop/settings_app.go`, `desktop/settings_app_test.go` | 用户全局配置读改写已通过独立事务锁串行；后台 provider 保存不再覆盖发送快捷键与新会话默认审批。并发回归连续 10 次、desktop 全量 Go 测试和实际 `debug-restart.bat` 重启保持验证通过。 |
| 模型设置简化与接入引导 | done | `developping/model-settings-simplification+2026-07-12` | Codex | `desktop/frontend/src/components/SettingsPanel.tsx`, `desktop/frontend/src/styles.css`, `desktop/frontend/src/locales`, `desktop/frontend/src/__tests__/model-settings-simplification.test.tsx`, `internal/config/fetch.go`, `docs/design-qa.md` | 模型页已改为连接状态优先、显式“添加模型服务”、默认模型和折叠高级设置；官方接入隐藏内部名称并支持失败重试。14 项专项契约、18 项既有设置契约、前端构建、desktop 全量 Go 测试及同屏视觉 QA 通过。 |
| CLI 会话标题启动对账 | done | `developping/session-title-startup-reconcile+2026-07-12` | Codex | `desktop/tabs.go`, `desktop/tabs_title_reconcile_test.go` | 启动构建 SessionList 时仅对空标题或自动来源的“新的会话”做 sidecar 级轻量对账；优先最新 session 的命名标题、CustomTitle、TopicTitle 与 Preview，不读取完整 JSONL，不覆盖手工标题。5 项专项回归、desktop 全量 Go 测试和 go vet 通过。 |
| 会话初始化内容展示解耦 | done | `developping/session-hydration-view+2026-07-12` | Codex | `desktop/frontend/src/lib/useController.ts`, `desktop/frontend/src/App.tsx`, `desktop/frontend/src/__tests__/tab-switch-hydration.test.tsx` | 历史加载状态已与 Controller/附属数据 hydrate 解耦，并按 sessionPath 记录包括空历史在内的解析结果；同一路径 agent:ready 可幂等复用，不再卸载 Welcome。72 项 hydration 回归、12 项新会话竞态回归、生产 TypeScript 检查和前端构建通过。 |
| 会话最近摘要分层展示 | done | `developping/session-recap-headline+2026-07-12` | Codex + WorkGround2 | `desktop/frontend/src/components/desktop-ui/IrisInfoComponents.tsx`, `TaskMemoryBar.tsx`, `desktop-ui-components.test.tsx` | 最近 assistant 原文先提炼为同源的新闻式 headline 与完整摘要；横条只展示 headline 并由 CSS 省略，Tooltip 展示未截断的完整摘要。过滤代码块、路径、命令和逐项清单，合并验证结果。159 项组件测试、类型检查及生产构建通过。 |
| 工作台侧栏窄屏切换 | done | `developping/workbench-sidebar-toggle+2026-07-12` | Codex + WorkGround2 | `desktop/frontend/src/App.tsx`, `desktop/frontend/src/styles.css`, `desktop/frontend/src/__tests__` | 工作台 SessionList 已与统一的 sidebarCollapsed 状态及 Ctrl+B 联动；宽屏可折叠，820px 以下展开为浮层，并提供可点击收起/展开按钮。App Chrome 74 项、Workbench 89 项专项测试及生产构建通过。 |
| 会话总结浮层显示全文 | done | `developping/full-session-summary-tooltip+2026-07-12` | Codex | `desktop/frontend/src/components/desktop-ui/IrisInfoComponents.tsx`, `desktop/frontend/src/__tests__/desktop-ui-components.test.tsx` | 保留总结清洗后的完整文本，行内继续由 CSS 省略，悬浮浮层展示全文。149 项专项组件测试、TypeScript 检查和前端生产构建通过。 |
| 桌面通用设置精简 | done | `developping/general-settings-cleanup+2026-07-12` | Codex | `desktop/frontend/src/components/SettingsPanel.tsx`, `desktop/frontend/src/locales`, `desktop/frontend/src/__tests__/settings-refresh-snapshot.test.tsx` | 已隐藏桌面风格、会话展示模式和底部信息栏配置；工作台保持默认且旧配置继续兼容；新会话审批改为“需要批准 / 自动批准 / 全部允许”并提供悬浮说明。类型检查、18 项专项回归、前端生产构建和配置默认值测试通过。 |
| 隐藏崩溃报告发送按钮 | done | `developping/hide-crash-report-send+2026-07-11` | Codex | `desktop/frontend/src/lib/crash.ts` | 崩溃弹窗仅保留复制；性能诊断上报入口不受影响。TypeScript 检查及崩溃上报测试通过。 |
| 小组件多皮肤 | done | `developping/widget-skins+2026-07-17` | Codex + WorkGround2 | `desktop/frontend/src/components/widget`, `desktop/frontend/src/assets/widget-mode/skins`, `desktop/frontend/scripts`, `desktop/frontend/src/components/SettingsPanel.tsx`, `desktop/settings_app.go`, `internal/config` | 保留现有消息与交互模型，增加 BP机、拍立得、电子宠物、录音机四套完整外壳，并保留经典皮肤；设置可即时切换并持久化，未知配置安全回退 classic。皮肤选择字段使用整行自适应网格；四套皮肤分别适配信息区、正文安全区、返回/工作区/新对话、操作/回复按钮及扫描线的布局与材质。九宫格切片通过 1px 向内重叠和外框裁切消除子像素拼接黑线。小组件布局、信息、设置、皮肤资源专项及前端生产构建通过。 |
| 会话切换保持后台运行 | done | `developping/session-switch-keeps-running+2026-07-19` | Codex | `desktop/tabs.go`, `desktop/tabs_order_test.go`, `internal/config/cli_capability.go`, `internal/config/cli_capability_test.go`, `internal/config/cli_capability_windows_test.go`, `PROJECT_FEATURE_MAP.md` | single-surface 切回时从 detached registry 重新挂载同一 Controller、SessionID、事件路由及待处理交互；新 session 的 Codex capability 探测统一使用 Windows `CREATE_NO_WINDOW`，失败缓存 5 分钟，避免每次启动重复拉起 `cmd.exe`。会话专项 race/20 次连续切换、Config 专项/race、Boot 回归、vet 和 Windows 构建通过。 |
| 发送为工作选中态强化 | `done` | `developping/work-send-selected-state+2026-08-01` | `Codex` | `desktop/frontend/src/components/desktop-ui/RuntimeConfigBar.tsx`, `desktop/frontend/src/styles.css` | 选中态使用实心强调色、勾选图标和“工作模式已选”文案。Work 合约 36 项、TypeScript、CSS、前端生产构建、Wails 正式构建及真实点击视觉验收通过；按用户要求不使用 WorkGround2 Worker。 |
| Session 输入区左右留白恢复 | `done` | `developping/composer-horizontal-spacing+2026-08-01` | `Codex` | `desktop/frontend/src/styles.css`, `desktop/frontend/src/__tests__/app-work-integration.test.tsx` | 页脚已有 `display: contents` 插槽包装，原直接子元素选择器无法命中输入框和配置栏；改用后代选择器恢复左右各 48px 留白。Work 合约 37 项、CSS、TypeScript、前端生产构建、Wails 正式构建及正式 EXE 视觉验收通过；按用户要求不使用 WorkGround2 Worker。 |
| Session 工作入口文案柔化 | `done` | `developping/work-entry-copy+2026-08-01` | `Codex` | `desktop/frontend/src/components/desktop-ui/RuntimeConfigBar.tsx`, `desktop/frontend/src/locales/zh.ts`, `desktop/frontend/src/App.tsx` | 将偏命令式的“发送为工作”调整为“作为工作开始”，选中后显示“将作为工作发送”。Work 合约 37 项、Iris 合约 17 项、TypeScript、Wails 正式构建及正式 EXE 双状态验收通过；按用户要求不使用 WorkGround2 Worker。 |
| 工作信息单项建议 | `done` | `developping/work-input-field-inference+2026-08-01` | `Codex` | `internal/work/input_inference.go`, `desktop/frontend/src/work/components/presentation/WorkInformationPanel.tsx`, `desktop/frontend/src/work/components/presentation/WorkDefinitionOverview.tsx`, 对应样式与测试 | 移除面板级批量推断入口，为定义字段和自定义字段统一增加无文字的小星光图标；单次只生成目标字段的候选草稿，保存前不改原值，失败可单项重试。后端定向测试、前端组件 33 项、TypeScript、CSS、Wails 正式构建及正式 EXE 五字段视觉验收通过；按用户要求不使用 WorkGround2 Worker。 |
| 多人协作连接与交互可用性 | `done` | `developping/collaboration-usability+2026-08-03` | `Codex` | `desktop/collab_*`, `desktop/frontend/src/collab`, 对应测试与文案 | 已增加可选本机 IP 的 `workground2://` 连接字符串导入导出、首次身份引导与稳定缓存；Agent 运行态改为固定高度动态走马灯卡，明确/疑似本机指令均进入可停止倒计时，逐条操作收敛为悬停图标与更多菜单；成员加入/重连改为轻通知。Client 每 95 秒向 Host heartbeat，失联判定保留四个周期，重连后自动同步缓存和 Outbox。协作前端 49/49、Desktop Collaboration 专项、Desktop vet、TypeScript、Vite 生产构建与独立 Wails 正式构建通过；`-clean` 首次构建仅因运行中的既有 `WorkGround2.exe` 锁定而失败，未关闭用户实例，非 clean 独立输出成功。按用户要求不使用 WorkGround2 Worker。 |
| Room 双身份头像与紧凑 Agent 面板 | `done` | `developping/collaboration-mentions+2026-08-04` | `Codex` | `internal/collab/model.go`、`internal/collab/service.go`、`desktop/collab_app.go`、`desktop/collab_persist.go`、`desktop/frontend/src/collab/{avatar.ts,CollaborationWorkspace.tsx,collab.css}` | “本人”和“Agent”各自可配置名字与头像；图片本机裁切压缩到 96px WebP/最大 64KiB，再以受限 data URL 幂等同步到 Room 快照、时间线头像和恢复文件，拒绝远程头像 URL。显式 AGENTS.md/SKILL.md 已移入弹窗，自动响应改为单行双开关，右栏真实窗口验收通过。验证：TypeScript、149/149 协作回归、`internal/collab`、desktop 协作定向测试与双方 vet 通过；Wails 产物 `desktop/build/bin/WorkGround2-room-agent-identity-ui.exe`。全量 desktop 套件另有既存 Work V2 文件存储用例失败，已显式记录。未使用 WorkGround2 Worker。 |
| Session RunBlock 心流微动画 | `done` | `developping/runblock-flow-motion+2026-08-14` | `Codex + subagent` | `desktop/frontend/src/components/desktop-ui/RunBlock.tsx`, `desktop/frontend/src/lib/runEvents.ts`, `desktop/frontend/src/store/run.ts`, `desktop/frontend/src/styles.css`, `desktop/frontend/src/__tests__` | 保留现有运行区结构与底部步骤轨道；新增搜索、读取、编辑、命令、测试、浏览器六类语义微动画及通用回退。RunBlock 缩窄并与正文左边缘对齐；成功采用确定性的依次锁定与揭晓，失败停在真实错误点并保留已完成证据。生产构建、CSS、TypeScript、Run/Store/Session/Composer 定向回归通过；用户明确要求 subagent 编码，禁止使用 WorkGround2 Worker。 |
| Session 石墨叠板与背景动效 | `done` | `codex/session-surface-redesign+2026-08-06` | `Codex` | `desktop/frontend/src/App.tsx`, `desktop/frontend/src/components/SessionSurface.tsx`, `desktop/frontend/src/components/work/WorkWorkspace.tsx`, `desktop/frontend/src/work/components/presentation/WorkInformationPanel.tsx`, `desktop/frontend/src/collab/CollaborationWorkspace.tsx`, `desktop/frontend/src/styles.css`, `desktop/frontend/src/__tests__`, `design-qa.md` | 已配置背景图提升为工作台底层，并统一 Session、Work、Room 的半透明叠板与窗体操作轨道。Work 填写信息已移除统一外框，改为当前卡加最多四张后卡的独立卡片栈；每张卡各有边框和投影，通过交替角度、位移、缩放形成扑克牌式散叠。面板 42/42、TypeScript、CSS、Vite production、真实预览计算样式与 Wails production 构建通过；正式 EXE 已更新。按用户要求未使用 WorkGround2 Worker。 |
